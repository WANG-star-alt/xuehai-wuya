---
name: "xuehai-wuya-requirements"
description: "学海无涯网站开发规范。当用户要求记住、整理、保存开发要求或修复 bug 时，必须写入此 skill。"
---

# 学海无涯 · 开发要求规范

## 网站基本信息

- **名称**: 学海无涯 (Xue Hai Wu Ya)
- **域名**: https://xuehai-wuya.pages.dev
- **仓库**: github.com/WANG-star-alt/xuehai-wuya
- **部署**: Cloudflare Pages (自动部署 main 分支)
- **技术栈**: 纯 HTML/CSS/JS，**数据驱动 SVG 自绘知识树**（已放弃 ECharts）

## 目录结构

```
xuehai-wuya/
├── index.html                       # 主页（封面 + 认知树 + 章节卡片）
├── assets/
│   ├── tree-data.js                 # 认知树节点数据（id/label/desc/time/children/href/color）
│   ├── cognition-tree.js            # 认知树渲染引擎（SVG 自绘 + 交互）
│   ├── cognition-tree.css           # 认知树样式（磨砂玻璃 + 节点卡片）
│   └── charts.js                    # 【已废弃】旧 ECharts 版，未使用
├── _shared/
│   ├── css/theme.css                # 所有子页面共享样式
│   ├── css/interactive.css          # 互动实验室组件样式
│   ├── js/float-pager.js            # 悬浮翻页岛
│   └── js/interactive.js            # 互动实验室组件引擎（18 种组件）
├── chapters/                        # 所有章节内容
│   ├── ai/                          # 智能篇 14 章
│   ├── network/                     # 网络篇 10 章
│   ├── gui/                         # 界面篇 10 章
│   └── elec/                        # 电学篇 11 章（待撰写）
└── .trae/skills/                    # 本 skill 存放处
```

**参考规范**: 遵循 `COGNITION_TREE_FORMAT_SPEC.md`（用户提供的知识树设计规范）

## 核心开发规范（每次必须遵守）

### 0. 三级命名规范（篇 → 章 → 节）★ 最重要

全站严格遵守 **篇 → 章 → 节** 三级结构，**最小单位是"节"，不是"篇"**。

| 层级 | 命名格式 | 示例 | 对应文件 |
|---|---|---|---|
| **篇**（顶层分支） | `XX篇` | 智能篇 / 网络篇 / 界面篇 / 电学篇 | 无独立页面，只是树上的分支 |
| **章**（第二层） | `第 X 章 · 名字`（**不带"篇"字**） | 第 5 章 · 神经网络 | `05-neural-network.html`（章总览页） |
| **节**（第三层） | `§ X.Y · 名字`（**不带"节"或"篇"字**） | § 5.1 · 神经元 | `05-1-neuron.html`（节正文页） |

**禁止事项**:
- ❌ 章级标题写成"第 5 章 · 神经网络**篇**"
- ❌ 节级标题写成"§ 5.1 · 神经元**篇**"或"§ 5.1 · 神经元**节**"
- ❌ 主页卡片 `<h3>` 写成"神经网络**篇**"
- ❌ 章总览页写"本章的六个**子篇**"（应写"本章的六**节**"）

**页面内标记**:
- 节页面 `chapter-head` 的 num 用 `§ X.Y · Section`（不是 `Sub-chapter`）
- 章页面 `chapter-head` 的 num 用 `Chapter · XX`
- 章总览页导航区标题用"本章的 X 节"

### 1. 章节结构规范

每个"章"和每个"节"都必须是一个**独立 HTML 页面**，放在 `chapters/<篇目录>/` 下。

**页面命名规则**:
- 章总览页: `XX-<title>.html` (如 `04-machine-learning.html`)
- 节正文页: `XX-Y-<title>.html` (如 `04-1-supervised.html`)

**每个页面必须包含**:
```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>§ X.Y · 标题 · 学海无涯</title>
  <link rel="stylesheet" href="../../_shared/css/theme.css">
  <!-- 用到互动组件时才加这一行 -->
  <link rel="stylesheet" href="../../_shared/css/interactive.css">
</head>
<body>
<article class="page">
  <!-- 顶部面包屑 -->
  <nav class="topbar">...</nav>
  <!-- 章节头：num 用 "§ X.Y · Section" 或 "Chapter · XX" -->
  <header class="chapter-head">...</header>
  <!-- 内容（含互动组件 div.lab） -->
  ...
  <!-- 底部翻页（.pager）：节用"上一节/下一节"，跨章用"上一章/下一章" -->
  <div class="pager">
    <a class="btn prev" href="...">← 上一节</a>
    <a class="home" href="../../index.html">☰ 主页</a>
    <a class="btn next" href="...">下一节 →</a>
  </div>
  <!-- 页脚格式：学海无涯 · <篇名> · § X.Y -->
  <div class="page-foot">学海无涯 · 界面篇 · § X.Y</div>
</article>
<!-- 用到互动组件时才加这一行，且必须在 float-pager 之前 -->
<script src="../../_shared/js/interactive.js"></script>
<!-- 必须引入悬浮翻页脚本 -->
<script src="../../_shared/js/float-pager.js"></script>
</body>
</html>
```

**禁止内联 `<style>`**——所有样式都放 `theme.css` / `interactive.css`，页面里不许写 `<style>` 块。

### 2. 悬浮翻页岛规范（重要）

**每个章节页面底部必须有 `.pager`**，并引入 `float-pager.js`。

**float-pager.js 会自动**:
- 克隆 `.pager` → 创建 `.pager-float`（悬浮岛）
- 给 body 加 `has-float-pager` 类（底部留白）

**悬浮岛样式**:
- 磨砂玻璃: `rgba(255,255,255,0.55)` + `backdrop-filter: blur(20px)`
- 圆角胶囊: `border-radius: 999px`
- 居中悬浮: `position: fixed; bottom: 18px; left: 50%; transform: translateX(-50%)`

**检查清单**（每次新建页面后）:
- [ ] 页面有 `.pager` 元素
- [ ] 页面引入了 `../../_shared/js/float-pager.js`
- [ ] 浏览器测试：滚动时悬浮岛始终可见

### 3. 认知知识树规范（当前实现 · SVG 自绘）

**数据位置**: `assets/tree-data.js` → `window.__cognitionTreeData`

**节点数据结构**（严格按 COGNITION_TREE_FORMAT_SPEC）:
```javascript
{
  id: "唯一稳定英文ID",        // 必填
  label: "节点显示名称",        // 必填
  desc: "节点简要说明",         // 必填
  words: "3200 字",            // 必填：内容字数（不再用"建议时间"）
  children: [],                // 必填（叶子节点空数组）
  href: "chapters/xxx.html",   // 可选：跳转链接
  color: "#4a6d8c"             // 可选：仅顶层分支设置，后代继承
}
```

**`words` 字段规范**（★ 已废弃"建议时间"）:
- **节节点** → 该节实际中文字数，如 `'3200 字'`；未上线的写 `'待撰写'`
- **章节点** → 该章所有节 + 总览页字数合计，如 `'约 1.8 万字'`
- **篇节点** → 该篇合计，如 `'约 6 万字'`
- 根节点 → 全站合计
- **理由**：阅读时间因人而异，字数是客观可核对的事实；也便于直接对照 §6 的 3000 字硬标准

**树结构**:
```
学海无涯 (root)
├── 智能篇 (color: #4a6d8c)
│   └── 第 1-14 章
├── 网络篇 (color: #556b3d)
│   └── 第 1-10 章
├── 界面篇 (color: #8a5a7a)
└── 电学篇 (color: #a0662c)
    └── 第 1-10 章
```

**顶层分支配色**:
- 智能篇 `#4a6d8c`（蓝）· 主页 banner `.vol-banner`
- 网络篇 `#556b3d`（绿）· 主页 banner `.vol-banner.net`
- 界面篇 `#8a5a7a`（紫褐）· 主页 banner `.vol-banner.gui`
- 电学篇 `#a0662c`（铜橙）· 主页 banner `.vol-banner.elec`

**节点规范**:
- ID 全树唯一，只用英文/数字/连字符
- 叶子节点必须有空 `children: []`
- 有内容的节点填 `href`，无内容的不填（视觉自动区分）
- 禁止五级嵌套：最多 4 级（root → 篇 → 章 → 子篇）
- 若子篇章已合并成一篇文章，树上只保留一个链接节点
- 例外：文章有清晰分段时可加锚点 `#section-id`，拆成多个子节点（如简史 5 个时代节点）

**视觉规范**:
- **文字前圆点颜色**（唯一状态标识）：
  - **深绿** `#556b3d` = 可跳转（有 `href`）
  - **浅绿** `#d4e2c8` = 可展开（无 `href` 但有子节点）
  - **浅灰** `#d8d4cb` = 叶子节点
- **不显示** `[+]/[-]` 加号按钮、右侧状态圆点、下划线等其他标识
- 节点卡片：184×54 圆角矩形，磨砂玻璃背景
- 连接线：真实 SVG 三次贝塞尔曲线，`<g class="edges">` 先绘制，`<g class="nodes">` 后绘制
- 三区结构：工具栏 + 树画布（磨砂玻璃）+ 详情面板（磨砂玻璃）

**交互规范**:
- **单击节点** → 若有子节点则展开/收起，同时更新详情面板
- **详情面板"打开完整学习章节"按钮** → 跳转 `href`
- **搜索** → 匹配 label + desc，只展开祖先路径，不展开后代
- **缩放**：滚轮、`＋/−` 按钮，范围 `0.55x ~ 2.5x`
- **拖动**：只在画布空白处拖动，不拖节点
- **展开层级**：Ⅰ 一级 / Ⅱ 二级 / Ⅲ 三级 / ⊟ 收起

**节点宽度滑块**（`#node-width-slider`）:
- 工具栏提供"框宽"滑块，范围 `120px ~ 420px`，默认 `184px`，步长 4
- `NODE_W` 是**可变状态**（不是常量），滑块改它后调 `render()` 重算布局
- `NODE_LABEL_MAX`（文字截断字符数）随宽度线性联动：`(NODE_W - 44) / 8.75`，下限 6 字
- `input` 事件只重绘（拖动流畅）；`change` 事件（松手）才 `fitCanvas()`，避免画面乱跳
- `#node-width-reset`（↺ 按钮）恢复默认 184px

**默认缩放规范**（重要）:
- **`fitCanvas()` 必须让树"正好装下"画布**，不能太小
- 上限 `1.4x`（小树自动放大填充画布），下限 `MIN_SCALE=0.55`
- 留 5% 内边距（`padding = 0.95`），不贴边
- SVG `viewBox` **必须用画布像素尺寸**（`0 0 clientW clientH`），不能用 world 尺寸——否则 SVG 自动缩放会和 `scene transform` 叠加，导致我的 scale 失效
- 初始化时用 `requestAnimationFrame × 2 + setTimeout 200ms 兜底`，确保 SVG 布局完成再计算
- window resize 时自动重新 fit

**索引系统**（初始化时构建）:
```javascript
const nodeIndex = new Map();     // id → node
const parentIndex = new Map();   // id → parent node
const depthIndex = new Map();    // id → depth
const branchIndex = new Map();   // id → 顶层分支节点（含 color）
```

**状态管理**:
```javascript
const collapsedNodes = new Set();  // 唯一的展开状态来源
let selectedNodeId;
let scale, translateX, translateY;
```

**移动端触摸支持**（★ 2026-08-07 补，改交互逻辑时不要退回去）:
- 鼠标事件（`mousedown`/`mousemove`/`mouseup`/`wheel`）**在触摸屏上完全不触发**，必须另绑 `touchstart`/`touchmove`/`touchend`/`touchcancel`
- **CSS 必须配套**：`.tree-canvas { touch-action: none; }`。不设的话浏览器会把单指拖动当成滚页面、双指捏合当成缩放整页，**手势被系统抢走，JS 收不到**
- `zoom(delta, cx, cy)` 支持焦点缩放：给了 `cx`/`cy` 就让该点在缩放前后保持不动（滚轮用鼠标位置，捏合用双指中心）
- 双指缩放**相对起始状态整体计算**（记 `pinch.d` / `pinch.s`，用 `pinch.s * (nd / pinch.d)`），**不要逐帧累乘**否则误差漂移
- 手指按在节点上时**跳过平移逻辑**，否则会吞掉节点的 `click`
- 判断是否点在节点上要用**手写的 `inNode()` 沿 `parentNode` 上溯**，因为 SVG 元素的 `closest()` 在个别旧版移动浏览器不可靠；且 SVG 要用 `getAttribute('class')` 而非 `classList`
- 移动端触摸目标**不小于 40px**（工具栏按钮 `min-height: 40px`）
- 加 `@media (hover: none)` 关掉移动端 hover 粘滞

### 3.5 深浅主题规范（★ 2026-08-07 定稿，已上线）

**最终方案：右上角按钮手动切换，不依赖系统与浏览器。**

**为什么不用 `prefers-color-scheme`（这是走了弯路才想清楚的）**：
用户手机上的「黑色模式」来自浏览器的**强制深色网页**功能。它能反转 CSS 背景，**但反转不了 SVG 的 `fill`** → 知识树节点底被染深、节点文字仍是深墨 `#2b2a26` → 深色字压深色底 → 字完全消失。而且各家浏览器行为不一致，跟它较劲改不干净。

**当前实现（三个文件配合）**：
1. `_shared/js/theme-toggle.js` —— 注入右上角圆形按钮，切换 `<html data-theme="dark">`，选择存 `localStorage`（键名 `xhwy-theme`）跨页面保持；同时同步 `<meta name="theme-color">` 让手机地址栏跟着变色
2. `_shared/css/theme.css` —— `.theme-toggle` 按钮样式 + `[data-theme="dark"]` 深色变量（只覆盖颜色，版式字体一律不动）
3. `assets/cognition-tree.css` —— 知识树独立深色块

**必须遵守**：
1. **每个 HTML 页面都要有** `<meta name="color-scheme" content="only light">`（插在 `viewport` 之后）。`only` 关键字拒绝浏览器强制反转。**新建页面必须带这一行**，并在 body 末尾引 `theme-toggle.js`（放在 `float-pager.js` 之后）。
2. 深色态自己声明 `color-scheme: only dark`，让滚动条等原生控件跟着变深，否则深色页面配浅色滚动条很割裂。
3. 深色规则用 **`[data-theme="dark"]` 属性选择器**，**不要用 `@media (prefers-color-scheme: dark)`** ——后者会让浏览器给无显式 `background` 的元素套深色 UA 样式，在浅色系统下冒出深色块（已踩过这个坑）。
4. 底色用**带暖调的深色而非纯黑**（`#1c1e1b` / `#23261f` / `#282b24`），保留纸感。
5. **无背景的容器必须显式给 `background`**（如 `.tree-canvas` 要写 `background: var(--bg)`），不能依赖 UA 默认值。
6. 首页 `index.html` 样式全在**内联 `<style>`** 里、且**不引用 `theme.css`**，所以按钮样式与深色变量要在首页单独写一份。改主题时容易漏掉首页。

**实测基准值**（深色下知识树，可用于回归验证）：节点文字 `rgb(237,240,230)`、节点底 `rgba(44,48,42,.95)`、画布底 `rgb(28,30,27)`、连线 `rgba(150,165,130,.42)`，对比度约 12:1。

**纯靠变量覆盖不到、必须单独处理的地方**：
- `pre` / `code` 底色与边框
- 表格斑马纹（深色下要反向，用 `rgba(255,255,255,.022)`）
- `mark` / `mark.key` 高亮句（浅色的淡黄底在深色下太刺眼，改低饱和暖底）
- `img` / `svg image` 加 `filter: brightness(.92)` 消除发光感
- **知识树需要独立暗色块**：`.node-bg` / `.node-title` / `.node-meta` / **`.edge`（连线最容易漏，浅底深线在深底同样消失）** / `.spec-dot` / `.branch-dot` / 详情面板全套
- 状态圆点的白描边（`stroke: #ffffff`）在深色节点上很扎眼，改成接近底色的深色描边；「可跳转」的深绿 `#556b3d` 在深底看不出，提亮到 `#93b06f` 一类

### 4. 主页卡片规范

**位置**: `index.html` → `.vol-banner` 下的 `.chapters`

**卡片状态**:
- **已上线**: `class="ch-card ready"`, `<span class="badge">已上线</span>`, `href="./chapters/ai/XX.html"`
- **建设中**: `class="ch-card wip"`, `<span class="badge">建设中</span>`, `href="#"`

**每完成一章，必须**:
1. 把 `wip` 改成 `ready`
2. 把 `badge` 文字从"建设中"改成"已上线"
3. 把 `href="#"` 改成真实链接

### 5. 翻页链规范

**每个页面底部 `.pager` 必须正确链接**，形成一条贯穿全篇的线性阅读路径：

```
章总览 → §X.1 → §X.2 → … → §X.n → 下一章总览 → §(X+1).1 → …
```

**方向文字用词**:
- 同章内节与节之间 → `← 上一节` / `下一节 →`
- 节 → 本章总览 → `← 上一节`，标题写"第 X 章 · 总览"
- 本章最后一节 → 下一章总览 → `下一章 →`，标题写"第 X+1 章 · 章名"

**首尾处理**:
- 每篇第一章总览页的"上一章" → `class="btn prev disabled"`（无 href），标题写"已是本篇首章"
- 最后一个已上线节的"下一节" → `class="btn next disabled"`（无 href），标题写"第 X 章 · 章名（建设中）"

**新增章节时必须回头改前一章末节的 next**——否则阅读链断在那里。

### 6. 内容规范

**每个节正文页必须包含**（缺一不可）:
- `p.lead` — 核心观点（带 `<mark class="key">` 高亮）
- `div.scene` — 生活场景类比（带 emoji，含 `.scene-tag` / `.scene-title` / `.link`）
- **8–20 个 `h3`** + `p` — 正文讲解（越多越细越好，见下方字数要求）
- `div.callout.analogy` — 至少一个譬喻框（承载核心概念的完整生活类比，见 §6.2）
- `ul.keylist` 或 `.table-wrap table` — 结构化信息
- **互动组件** `div.lab` — 见 §6.5，至少一个 `quiz`
- `div.callout` — 结尾 Recap 收束
- `div.pager` — 翻页

**章总览页必须包含**: `p.lead` + "本章要回答什么" + `div.callout.analogy` + `div.cards`（各节导航卡片）+ "学之前先记住这一点" `ul.keylist` + `div.pager`。章总览页**不放互动组件**。

**字数要求（只算中文字符，不含标点数字英文）**:

| 页面类型 | 下限 | 建议 | 说明 |
|---|---|---|---|
| **节正文页** | **3000 字** | **越多越好，无上限** | 6000–9000 字是当前已达标文章的常态，值得对标 |
| **章总览页** | 600 字 | 600–1200 字 | 做导航和总览，不必长 |

**★ 用户核心原则（优先级高于一切）**:
1. **完成度第一** —— 把所有节写完、写全，比把某几节打磨到极致更重要。不要因为追求单篇完美而拖慢整体进度。
2. **字数越多、内容越详细越好** —— 3000 字只是**及格线不是目标**。不要主动压缩篇幅、不要为了省 token 写得简略。能多举一个例子就多举，能多拆一层原理就多拆，能多加一张对比表就多加。
3. **专业名词必须配大白话 + 生活类比** —— 见下方 §6.2，这是写作的第一准则。

### 6.2 大白话与生活类比规范（★ 第一准则）

**读者假设**：一个聪明但零基础的人。他不懂任何术语，但愿意认真读。**任何需要"你应该已经知道 X"才能读懂的句子，都是不合格的**。

**硬规则一 · 术语首次出现必须当场翻译**

每个专业名词第一次出现时，必须紧跟一句大白话解释——不能留到后面、不能指望读者自己查。

```
❌ 反例：Transformer 采用自注意力机制替代了 RNN 的循环结构。
✅ 正例：Transformer 用一种叫「自注意力」的办法取代了 RNN 的老路子。
       所谓自注意力，说白了就是：读一句话时，让每个词都回头看一眼句子里
       其他所有词，自己判断「谁跟我关系最大」——而不像老办法那样只能
       一个字一个字往后传话。
```

推荐句式（挑一个用，不要每次都一样）：
- 「所谓 X，说白了就是……」
- 「X 这个词听着玄，其实就是……」
- 「换成大白话：……」
- 「X 干的活儿，相当于……」

**硬规则二 · 每个核心概念配一个生活场景类比**

类比必须取材于**厨房、菜市场、快递、餐厅、公交地铁、医院、学校、办公室、装修、洗衣、停车**这类人人有实感的场景。**禁止用一个术语解释另一个术语**。

| 概念 | 好类比（可直接复用） |
|---|---|
| CPU / 内存 / 硬盘 | 厨师 / 案板 / 冰箱 |
| 缓存 | 手边的调料罐，不用每次跑储藏室 |
| 带宽 vs 时延 | 水管多粗 vs 水流过去要多久 |
| TCP 三次握手 | 打电话：「听得到吗」「听得到，你呢」「我也听得到」 |
| 端口号 | 小区门牌号里的「几号房」 |
| DNS | 通讯录：你只记名字，它帮你查号码 |
| 神经网络训练 | 调收音机旋钮，一点点转到声音最清楚 |
| 过拟合 | 只会做例题、换个数字就不会的学生 |
| Token | 乐高积木，句子是拼出来的 |
| 梯度下降 | 蒙着眼下山，用脚感觉哪边是下坡 |
| 事件循环 | 酒店前台：坐着等，来事就处理 |
| 盒模型 | 快递包裹：商品、气泡膜、纸箱、箱子之间的空隙 |
| 矢量图 vs 位图 | 绣花图纸 vs 已经绣好的成品 |

**硬规则三 · 抽象数字要换算成可感知的量**

```
❌ 175B 参数、13 万亿 token、16.7 毫秒
✅ 1750 亿个参数——如果每个参数写在一张 A4 纸上，纸摞起来有 17 公里高
✅ 13 万亿 token——相当于把维基百科全文读上几千遍
✅ 16.7 毫秒——比你眨一次眼（约 100 毫秒）还要短六倍
```

**硬规则四 · 缩写第一次出现要给全称 + 中文 + 怎么读**

```
✅ RLHF（Reinforcement Learning from Human Feedback，"基于人类反馈的强化学习"）
✅ MTU（Maximum Transmission Unit，最大传输单元——一个包最多能装多少字节）
```

**硬规则五 · 每篇至少一个 `div.callout.analogy` 譬喻框**，用来承载最重要那个概念的完整类比（可以写长，三五句都行）。已有的 `div.scene` 是开篇场景，`callout.analogy` 是概念类比，**两者都要有，不能互相替代**。

**自查三问**（写完每个 h3 都问一遍）:
1. 这段里有没有没解释就直接用的术语？
2. 这段的核心概念，一个完全不懂的人读完能不能用自己的话讲出来？
3. 有没有哪个数字是抽象的、没换算成可感知的量？

**因此写作时应当**:
- 每篇尽量写到 **8–20 个 `h3`**，每个 h3 下 2–4 段实质讲解
- 主动补充：真实数据、具体年份、人物姓名、论文名、产品版本号、成本数字、性能指标
- 主动补充：对比表格、ASCII 结构图、可运行代码示例、命令行实操、踩坑清单
- 主动补充：历史脉络（谁在哪一年提出、为什么当时没成功）、正反双方观点、常见误解澄清
- **唯一的红线是不许灌水**——空话、重复表述、无信息量的过渡句不算字数贡献

**字数自查命令**（写完必须跑）:
```python
# 去掉 script/style/lab 组件后统计中文字符
s = re.sub(r'<script.*?</script>|<style.*?</style>', '', html, flags=re.S)
s = re.sub(r'<div class="lab"[^>]*></div>', '', s)
len(re.findall(r'[\u4e00-\u9fff]', re.sub(r'<[^>]+>', '', s)))
```

**代码示例**: 用 `<pre><code>` 包裹，HTML 标签要转义（`&lt;` / `&gt;`）。`theme.css` 已有全局 `pre` 样式，不要在页面里另写。

### 6.5 互动实验室规范（★ 重要 · 每篇都要考虑）

**核心原则**: 讲到"可以动手试"的概念时，**必须配一个互动组件让用户亲自玩**，而不是只用文字描述。讲颜色就给色盘，讲布局就给可拖的盒模型，讲帧率就给能调速的动画。

**引入方式**（需要用组件的页面才引，不用的不引）:
```html
<!-- head 里，theme.css 之后 -->
<link rel="stylesheet" href="../../_shared/css/interactive.css">
<!-- body 末尾，float-pager.js 之前 -->
<script src="../../_shared/js/interactive.js"></script>
```

**使用方式**: 只写一个空 div，JS 自动扫描 `data-lab` 并渲染，无需手写内部结构。
```html
<div class="lab" data-lab="color-picker"></div>
```

**现有 18 个组件**:

**通用 · 任何篇都能用**

| `data-lab` | 组件 | 适合放在讲什么的地方 |
|---|---|---|
| `quiz` | 单选小测验 + 判定 + 解析 | **每一节的结尾都要有**，检验理解 |

**界面篇专用**

| `data-lab` | 组件 | 适合放在讲什么的地方 |
|---|---|---|
| `color-picker` | HSL 调色盘 + 三种色值 + 预设色 | 颜色、RGB/HSL、配色、主题色 |
| `contrast` | 对比度检查器 + WCAG 徽章 | 可读性、无障碍、深色模式 |
| `box-model` | 盒模型四层可拖演示 | 盒模型、margin/padding、布局计算 |
| `flexbox` | Flex 属性下拉 + 实时代码输出 | Flexbox、一维布局、对齐 |
| `pixel-zoom` | 网格数可调的像素化圆 | 像素、分辨率、锯齿、抗锯齿 |
| `event-flow` | 三层嵌套点击 + 冒泡日志 + stopPropagation 开关 | 事件冒泡、事件委托、事件循环 |
| `fps` | 帧率可调的小球动画 | 帧率、60fps、卡顿、动画性能 |
| `converter` | PPI / DPR 换算器 | 分辨率、DPI、@2x 图、Retina |
| `cmd-break` | 命令行三段结构拆解器 | CLI、命令语法、选项与参数 |

**智能篇专用**

| `data-lab` | 组件 | 适合放在讲什么的地方 |
|---|---|---|
| `tokenizer` | 分词器模拟 + token 计数统计 | Token、分词、上下文窗口、计费 |
| `temperature` | 温度调节 + softmax 概率条形图 | Temperature、Top-p、采样策略 |
| `embedding` | 二维向量夹角 + 余弦相似度 | Embedding、语义搜索、RAG、CLIP |
| `neuron` | 神经元加权求和 + 四种激活函数 | 神经元、权重偏置、激活函数、非线性 |

**网络篇专用**

| `data-lab` | 组件 | 适合放在讲什么的地方 |
|---|---|---|
| `subnet` | 子网掩码计算器 + 32 位可视化 | IP、子网划分、CIDR、掩码 |
| `handshake` | TCP 三次握手分步演示 + 状态机 | TCP、握手挥手、连接建立 |
| `latency` | 时延账本（RTT + 传输时间分解） | 时延、带宽、RTT、CDN、性能指标 |
| `status-code` | 13 个 HTTP 状态码点击查询 | HTTP、状态码、错误排查 |

**小测验写法**（唯一需要传参数的组件）:
```html
<div class="lab" data-lab="quiz"
     data-q="题干，可含 <code>行内代码</code>"
     data-opts="选项A|选项B|选项C|选项D"
     data-answer="1"
     data-explain="答案解析——不只说对错，要讲清为什么。"></div>
```
`data-answer` 是**从 0 开始**的正确选项下标。

**放置原则**:
- 互动组件插在**对应概念讲完的那一段之后**，不要堆在文末
- 每篇 **1-3 个**互动组件为宜，`quiz` 固定放在结尾 Recap 之前
- 组件是"讲完再练"，不能代替文字讲解——先把道理说明白，再让用户动手验证

**扩展新组件**: 在 `interactive.js` 的 `builders` 注册表里加一个 `'名字': build函数`，配套样式加到 `interactive.css`。函数签名 `function(box)`，box 就是那个 `.lab` 容器。

### 7. 缓存规范

**每次修改 `tree-data.js` / `cognition-tree.js` / `cognition-tree.css` / `index.html` 后，必须更新缓存号**:

```html
<link rel="stylesheet" href="./assets/cognition-tree.css?v=ct-N" />
<script src="./assets/tree-data.js?v=ct-N"></script>
<script src="./assets/cognition-tree.js?v=ct-N"></script>
```

`N` 递增（**当前 `ct-35`**，每次改完 +1）。旧版 `kmap-XX` 已废弃。

**★ 缓存号必须覆盖所有静态资源（2026-08-07 血泪教训）**

改了 `theme.css` 后用户连说两次「还是没改」，根因是：**正文页引用 CSS 一直没带版本号**（`href="../../_shared/css/theme.css"`），浏览器直接用缓存，新文件压根没下载。首页的 `cognition-tree.css` 有 `?v=ct-NN` 所以那部分改动能看到，`theme.css` 从来没带过——**这是长期存在的漏洞，只因以前没改过 theme.css 才没暴露**。

现在全站 `theme.css` / `interactive.css` / `interactive.js` / `float-pager.js` / `theme-toggle.js` 都带 `?v=ct-NN`。**改任何共享资源后必须递增缓存号**，用脚本批量替换 `ct-旧` → `ct-新`（98 个页面一次搞定）。

### 7.5 交付前自检规范（★ 每次改完必做）

**★★★ 铁律：派出去的 agent 必须先落盘，再返回。绝不允许成果只存在于回执里（2026-08-08 立，已因此丢失整章核实成果）**

**事故经过**：我同时派了两个核实 agent（第 12 章生态、第 13 章风险），要求它们「只做核实，不要写任何文件」，把结果写在回执里返回。结果第 12 章回执只回来 §12.1 就被截断，**第 13 章回执整份丢失**——那是包含几十条 arXiv 编号、法律判例状态、OWASP 条目的完整核实成果，全部蒸发，只能重做。

**根因不是运气差，是我明知故犯**：本 skill 早在 2026-08-07 就记录了「故障一：agent 回执丢失」这一已知故障，我却仍然把大量不可重建的成果放在回执这个**已知会丢的通道**上。

**从此强制执行**：

| 场景 | 禁止 | 必须 |
|---|---|---|
| 派**核实/调研** agent | ❌ 说「不要写任何文件」 | ✅ 要求它把结果写成 `facts-<章节>.md` 存到**临时工作目录**，回执只写一句「已存至 X，共 N 条」 |
| 派**写作** agent | ❌ 让它攒完一次性输出 | ✅ 先 Write 建骨架，再多次 SearchReplace 追加（每次 3~4 个 h3） |
| agent 回执异常/截断 | ❌ 立刻重派 | ✅ 先查磁盘文件与时间戳判断真实进度，往往只需补尾部 |

**「不要写文件」这句话只在一种情况下才对**：任务产出极短（三五行结论）、且丢了重做成本很低。**只要成果是「重建代价高」的（大量出处链接、编号、原文引句），一律必须落盘。**

落盘位置用**临时工作目录**（不污染用户工作区）：`c:\Users\13459\.trae-cn\work\<session>\facts-*.md`。核实类产物属中间件，不进 git。

**批量操作优先用脚本**——新增/修改多个页面时，写一个临时 Python 脚本放在临时工作目录跑，不要逐个手改。避免高频工具调用触发 `AccountRateLimitExceeded`（HTTP 429）。

**注意**：这条只针对**工具调用频率**，不是让内容写得少。文章篇幅一律按 §6 的"越详细越好"执行。省调用次数的正确做法是**按章打包派 agent**（一次给 5–6 节的任务），而不是缩减每篇内容。

**★ 防 429 限流铁律（已多次踩坑，必须遵守）**

`AccountRateLimitExceeded`（HTTP 429）的根因是**瞬时请求量叠加**，不是总量。已验证的触发条件与对策：

| 触发行为 | 后果 | 正确做法 |
|---|---|---|
| **同时派 3 个 agent** | 请求量 ×3，几乎必然 429 | **一次最多派 2 个**，写作类任务**建议一次 1 个** |
| 逐个文件手动 Read + Edit | 调用次数爆炸 | 写一个临时 Python 脚本批量处理 |
| 多条独立命令分开发 | 每条都是一次往返 | 用 `;` 串成一条命令（如同步+提交+推送三合一） |
| 反复跑校验脚本确认 | 无谓往返 | 校验脚本一次跑完输出全部结果 |

**派 agent 的安全节奏**：
- 写作类（每篇 6000–10000 字，agent 内部读写频繁）：**一次 1 个 agent，写 3–5 节**
- 若确实要并行：**最多 2 个**，且两个任务的文件目录不重叠
- **绝不要 3 个及以上并行**——本项目已因此失败两次（2026-08-01、2026-08-02）

**遇到 429 后的正确处置**：
1. **不要立刻重试**，先用**一条**命令确认磁盘上的实际进度（很可能任务已完成，只是回执丢了）
2. 确认已完成的部分**立即提交推送**保住成果
3. 剩余部分**降低并发**（改成 1 个 agent）继续，不要原样重试

**★ 浏览器验证要一次取全数据（2026-08-07 又踩一次 429）**

用浏览器实测主题切换时，我做了导航 → 点击 → 两次 JS 求值 → 截图 → 读图，连续 6 次调用，撞了限流。**正确做法是一次 `browser_evaluate` 把所有要查的值一起返回**：

```javascript
// 一次拿全：主题状态 + 关键元素计算样式
const r = document.documentElement;
return JSON.stringify({
  dataTheme: r.getAttribute('data-theme'),
  bodyBg: getComputedStyle(document.body).backgroundColor,
  nodeTitleFill: getComputedStyle(document.querySelector('.node .node-title')).fill,
  nodeBgFill: getComputedStyle(document.querySelector('.node .node-bg')).fill,
  saved: localStorage.getItem('xhwy-theme')
});
```

另注意：**浏览器工具的 `browser_click` 可能被派发两次**（我点一次切换按钮，结果切过去又切回来，误判为「功能没生效」）。用 `browser_evaluate` 里直接 `el.click()` 更可控。

**★ 改 CSS / JS 前必须先核实标识符真实存在（2026-08-07 定，已犯过两次）**

我曾凭空写出项目里不存在的类名和变量，都是在核实环节才抓出来的：
- 写了 `.node .node-hit { stroke-width: 14px }` 想放大触摸区 —— **`node-hit` 这个类压根不存在**，节点其实是 `<rect class="node-bg">` 铺满整框、点击区域本来就够大
- 写了 `--ai` / `--network` / `--gui` / `--elec` 四个品牌色变量想在深色下提亮 —— **这四个变量不存在**，四篇颜色是各页面直接硬编码的（`#4a6d8c` 蓝 / `#556b3d` 绿 / `#8a5a7a` 紫 / `#a0662c` 铜）

**所以定下规则**：写任何 CSS 选择器或变量引用前，先 grep 确认它在 JS/HTML 里真实存在。
- 类名：搜 JS 里的 `class: 'xxx'` 与 HTML 里的 `class="xxx"`
- 变量：搜 `--xxx:` 的**定义处**，不能只看 `var(--xxx)` 的使用处

**改主题/样式后必跑的四项校验**（写成一个脚本一次输出）：
1. CSS 括号配平（**先去注释**再数 `{` `}`）
2. 暗色规则引用的每个类名都能在 JS / HTML / 浅色规则中找到
3. 暗色块定义的每个变量在浅色 `:root` 里都有对应（防止凭空造变量）
4. `color-scheme` 覆盖率达到全站页面数

**★ 推送验证规范（2026-08-07 补）**

- `hostfile_replace_entries: Operation not permitted` 是 SSH 更新 `known_hosts` 的**权限警告，与推送成败无关**，每次都会出现。
- **不要用 `git push -q`**——那会把 `main -> main` 的成功回显压掉，只剩这行警告，看着像失败（已因此让用户误以为没推上去）。
- 推送后**额外跑一条** `git ls-remote origin refs/heads/main`，与本地 `git log --oneline -n 1` 对比 commit hash 确认一致，再向用户汇报。

**★ 真机验证优先于模拟（2026-08-07 补）**

- 触摸交互、暗色模式这类**依赖设备与系统行为**的改动，让用户在真机上试**比在无头浏览器里派发合成事件可靠得多**。真机的多点触控事件顺序、浏览器手势拦截、系统强制反转行为都难以模拟。
- 改动是纯增量时（只新增事件监听、不动原有逻辑），风险低，可直接推送让用户验证。
- 但**必须明确告诉用户要留意哪几点**，给出可操作的检查清单。例如触摸改动要问：双指缩放会不会跳、点节点还灵不灵、页面还能不能正常上下滚。

**★ 规范状态追踪机制（树上的徽标）**

树数据每个叶子节点带一个 `spec` 字段，在知识树节点右上角显示徽标：

| 值 | 徽标 | 含义 |
|---|---|---|
| `'pass'` | 绿色「已达标」 | 已按当前制作规范改造完成 |
| `'todo'` | 橙色「待改造」 | 字数 / h3 / 大白话 / 生活类比等未达标 |
| 无此字段 | 不显示 | 尚未撰写 |

树的图例区还会自动统计「已达标 N / 已撰写 M（百分比）· 未撰写 K」。

**维护脚本**：`sync_spec.py`（放在临时工作目录），三种用法：

```
python sync_spec.py            # 按当前规范全站检查，自动写入 pass/todo
python sync_spec.py --report    # 只看报告，不改文件
python sync_spec.py --reset     # 把所有 pass 强制降为 todo（★ 设定新标准时用）
```

脚本顶部有一段阈值常量（`MIN_WORDS` / `MIN_H3` / `PLAIN_PER_KILO` / `MIN_LIFE` 等），**改标准时先改这里**，再跑一次即可。

**★ 用户设定新标准时的固定流程**（务必照做）：
1. 把新标准写入本 skill 的 §6 / §6.2
2. 改 `sync_spec.py` 顶部的阈值常量
3. 跑 `python sync_spec.py --reset` —— 全站降为「待改造」，进度归零
4. 逐批改造，每批完成后跑 `python sync_spec.py` 重新评定
5. 改造完的节点自动变绿，树上一眼能看出还剩多少

这样用户随时能在树上看到"新标准下的真实完成度"，而不是靠记忆或翻对话。

**每次交付前必须跑一遍自检**，检查项：
- [ ] **跑 `sync_spec.py` 更新徽标状态**（让树上的进度与实际一致）
- [ ] **术语都配了大白话解释？核心概念都有生活类比？抽象数字都换算了？**（§6.2 第一准则）
- [ ] 每个页面都有 `.pager` 且引入了 `float-pager.js`
- [ ] 用了互动组件的页面，`interactive.css` 和 `interactive.js` 都引了；没用的页面不要引
- [ ] 所有 `data-lab` 的值都在 `interactive.js` 的 `builders` 注册表里
- [ ] 每个 `quiz` 的 `data-q` / `data-opts` / `data-answer` / `data-explain` 四项齐全，且 `data-answer` 下标不越界
- [ ] 没有内联 `<style>` 块
- [ ] 命名符合 §0：节标题不带"节/篇"字，章标题不带"篇"字
- [ ] 树数据里新上线的节点已加 `href`（否则圆点还是灰的）
- [ ] 主页对应卡片已从 `wip` 改成 `ready`
- [ ] 缓存号已递增

**注意**: 校验 quiz 参数的正则不能用 `[^>]*`——`data-q` 里可能含 `<code>` 标签，会被提前截断。要用 `(.*?)></div>` 配 `re.S`。

**浏览器验证**：需要验证交互逻辑时，优先用一次 `browser_evaluate` 批量取回多个断言结果，不要反复截图和抓页面快照（快照返回上万字，极易触发限流）。

### 7.6 法律与安全类内容的写法规范（★ 2026-08-08 立，写第 13 章时定）

**适用范围**：凡是正文出现司法辖区、法院、判例、法规、案件状态、安全漏洞的段落，一律按本节执行。这类内容的特点是**流传最广的说法错得最离谱**，而且读者可能据此做出真实决定，所以标准要比其他章严。

**规则一 · 显著位置必须写「本文不构成法律建议」**

- 章总览页与涉法节页面都要有，且**放在 `.lead` 之后的第一个 `div.callout` 里**，不能只塞在文末。
- 同一节里法律内容分散出现时，**在关键处重复一次**（第 13 章的 §13.3 与 §13.4 各写了两遍）。
- 配套三句提醒：**法律有辖区** / **案件状态随时变化** / **地区法院裁定 ≠ 终局判决**。

**规则二 · 判例状态必须用四个词之一，不许含糊**

| 状态 | 英文 | 能不能当结论 |
|---|---|---|
| 审理中 | pending | 完全不能，只能说「有人在争」 |
| **部分简易判决** | partial summary judgment | 是真实裁定，但**只覆盖它明确处理的争点** |
| **上诉中** | on appeal | **不能当定论**，上级法院可推翻，不构成有约束力先例 |
| **和解结案** | settled / terminated | **特别不能**，法院未就实体争议下判断，**通常不确立先例，也不构成责任承认** |

**每起案件必须写全四项**：司法辖区（含法院简称如 S.D.N.Y. / N.D. Cal. / D. Del.）+ 案件编号 + 当前状态 + 状态核实日期。

**本章已核实并写入正文的五起（可直接复用，但用前务必重新核实日期）**：
- **NYT v. OpenAI** —— S.D.N.Y.，合并入 `In re OpenAI, Inc. Copyright Infringement Litigation, No. 1:25-md-03143`（MDL 2025-04-11 成立，Judge Sidney H. Stein）——**审理中**
- **Authors Guild v. OpenAI** —— 已并入同一 MDL ——**审理中**
- **Andersen v. Stability AI** —— N.D. Cal., `No. 3:23-cv-00201-WHO`（Judge William H. Orrick）——**审理中**；已有 700 F. Supp. 3d 853 (2023) 与 744 F. Supp. 3d 956 (2024) 两项**程序阶段**裁定
- ★**Thomson Reuters v. Ross Intelligence** —— 地区法院 D. Del., `No. 1:20-cv-613-SB`（Judge Stephanos Bibas），2025-02-11 就 fair use 作出**对原告有利的部分简易判决**（Document 770，认定 `Ross infringed 2,243 headnotes`）；**但现在第三巡回上诉法院上诉中，`No. 25-2153`，2026-06-11 已口头辩论、尚未判决**。§1292(b) 中间上诉许可 2025-05-23 获批（3rd Cir. No. 25-8018）。**绝不能写成「已定论」或「已成先例」**
- ★**Bartz v. Anthropic** —— N.D. Cal., `No. 3:24-cv-05417-AMO` ——**和解结案（2026-07-20 terminated），不是判决**

**规则三 · 取不到状态就明写「未取得」，绝不凭记忆补**

法律话题上，**一个凭记忆写出的案件状态比不写更有害**——读者会拿它当依据。第 13 章因此留白了：Getty v. Stability AI（美国 D. Del.）、Getty v. Stability AI（英国高等法院）、美国 TAKE IT DOWN Act 是否生效。**同名当事人的跨辖区案件（如 Getty 两案）必须分开写，且明说「结论不能互相搬用」。**

**规则四 · 法规必须区分「生效」与「适用」，并给完整分阶段表**

`entered into force`（生效）≠ `became applicable`（适用）。EU AI Act（Regulation (EU) 2024/1689）是标准反例：**2024-08-01 生效，但分阶段适用** —— 禁止性规则 2025-02-02 / GPAI 2025-08-02 / 透明度（含深伪标识）2026-08-02 / 第 9 项禁止实践 2026-12 / 高风险 Annex III 2027-12-02 / 高风险 Annex I 2028-08-02。AI Omnibus 修正案 2026-07-27 生效，EUR-Lex 当前合并版本日期 2026-07-27。**「已于 2024 年 8 月全面生效」是错的，错在「全面」。**

中国两部：《生成式人工智能服务管理暂行办法》（七部门，网信办令第 15 号）**2023-08-15 生效**；《人工智能生成合成内容标识办法》（国信办通字〔2025〕2 号，2025-03-07 发布）**2025-09-01 生效**，要求**显式标识 + 隐式标识（元数据）**并行。

**规则五 · 论文一律给 arXiv 编号 + 明确标注是否同行评议**

正文里 `arXiv 2401.11817` 这样写编号，紧跟 `（会议/期刊名，同行评议）` 或 `（arXiv 预印本，本文未取得其同行评议记录）`。**「预印本」与「已发表」必须让读者一眼分清**，因为前者可能有争议结论（第 13 章的 2401.11817 就是）。

**规则六 · 形式化结论必须写限定条件，不许外推成普适真理**

第 13 章最重要的一处：**arXiv 2401.11817「幻觉不可消除」的五条限定**——① 结论成立于论文自建的**形式化世界**；② 依据是计算学习理论（LLM 学不完所有可计算函数）；③ **从形式世界到现实世界那一步是外推，不是形式化证明，有争议**；④ 未排除在**受限任务**上做到零幻觉；⑤ 作者自己在讨论缓解手段的效果。**把它写成「AI 永远不可能准确」是全称与特称的混淆。**可复用的讲法：**「不存在能测宇宙中任何温度的温度计」这个结论完全正确，但丝毫不影响腋下体温计在 35–42 ℃ 好用。**

**规则七 · 数字必须绑定「哪个模型 + 哪个测试集 + 哪一天」，并保留原文限定词**

- `up to` / `maximum`（**上限，不是均值**）：Gender Shades 的 34.7% 与 0.8% 两处都带这个词，删掉「最高达」就是数字滥用。
- 区分**误报（false positive）与漏报（false negative）**：NISTIR 8280 说的是**误报率**差 10 到 100 倍以上，不是「整体错误率」。
- **测的是哪个任务**：Gender Shades 测「商用性别分类」，NISTIR 8280 测「人脸识别」，**混引即错**——而且这正是 NIST 自己出面纠正过的那处（原文 `Those studies did not evaluate face recognition algorithms, yet the results have been widely cited to indict their accuracy.`）。
- **取不到就不给**：本章拒绝给出「幻觉率 X%」「RAG 降低 XX% 幻觉」「FaceForensics++/DFDC 冠军方案下降多少」「拟声需要几秒素材」——一律明写未取得。

**规则八 · 安全类结论优先引厂商官方原文，取不到就留白**

「无法根治」这种强断言必须有官方原话撑。第 13 章能写是因为拿到了 Anthropic Computer Use 文档原文：`In some circumstances, Claude will follow commands found in content even if it conflicts with the user's instructions.` 以及它**在同一份文档里两次**建议 `isolate Claude from sensitive data and actions`。**而 OpenAI 侧未取得同类原文，就只写「未取得」，不代它下结论**——「某厂商承认了某件事」是关于事实的断言，不能靠推理补全。MCP 官方安全警告原文亦未取得，只列关键词（confused deputy / token passthrough / SSRF / 会话劫持 / 最小权限）供读者自查。

**规则九 · 版本号类清单要标版本，取不到就说取不到**

OWASP Top 10 for LLM Applications 的**最新版本号本次未取得官方确认**，正文明写「所列十项基于 v1.0 / v1.1，正式引用请去 OWASP 官网核对」。同类：C2PA 规范**截至 2026-08-08 为 2.4**（中文资料常见的「最新 2.3」已过期），且**其 `Guidance for AI/ML` 一项仍为 2.3，与其他组件版本不同步**——这种「同一标准内部版本不齐」值得写成教学点。

**规则十 · 研究结论要区分「存在性证明」与「威胁评估」**

arXiv 2510.07192（Anthropic 联合 UK AI Security Institute 与 Alan Turing Institute，2025-10-09，预印本）的 250 份文档投毒结论，**必须连作者自己的两条限定一起转述**：① `Our study focuses on a narrow backdoor (producing gibberish text) that is unlikely to pose significant risks in frontier models.`；② `It remains unclear how far this trend will hold as we keep scaling up models.` **它是存在性证明，不是威胁评估**——「250 个样本就能完全控制模型」是错的。同理 Many-shot Jailbreaking 的「61% → 2%」是**特定设定下特定措施**的结果，且论文明写微调 `merely delayed the jailbreak`（**推迟，不是阻止**）。

**规则十一 · 风险章的基调：不许危言耸听**

每节结构固定为**先把风险讲透（含限定条件）→ 再给可操作应对清单**。第 13 章五节的结论全部落在「所以你该怎么做」，**没有一节落在「所以别用了」**。可复用的贯章类比：**菜刀**——「厨房里没有一条规则是『别用刀』，全都是『在这种情形下这样握』」（猫爪握法、刀口不朝上放水槽、递刀递刀柄、生熟分板）。另一个好用的：**洗手消灭不了所有细菌，但这从来不是不洗手的理由。**

### 7.7 方法论章与收束章的写法规范（★ 2026-08-08 立，写第 14 章时定）

**适用范围**：凡是「不讲技术原理、只讲怎么用与什么态度」的章节（第 14 章「与 AI 共处」是第一个），以及任何**篇末收束章**，一律按本节执行。这类章节的失败方式非常固定：**通篇正确但一句都用不上**。

**规则一 · 反空话铁律：每条建议都必须能被抄走**

方法论章最大的风险是写成「要有批判性思维」「要保持独立判断」这种**正确但零信息**的句子。判断标准很简单：**读者能不能把这一段直接复制到对话框里用？**

| 空话 | 可用的写法 |
|---|---|
| 「要用费曼学习法」 | 给一段完整的提问模板，**并逐条解释每条规则堵的是哪个漏洞** |
| 「要批判性思考」 | 三步验证法：定性（六类分级表）→ 定源（三个动作，成本递增）→ 定责（五档，按谁承担后果） |
| 「要小心 AI 写的代码」 | 五级分级表（L0~L4）+ 九项审查清单 + 五条提问改法 |
| 「有些决定不该交给 AI」 | 四条标准（责任归属 / 不可逆 / 价值判断 / 他人利益）+ 三十秒四问检查 |

**可复用的具体手法**：写完一条建议就问「这句话的下一句能不能是『具体怎么做』」。答不出来就删掉。第 14 章因此每节都带 `<pre><code>` 提示词模板（§14.1 五个、§14.2 四个、§14.3 一份九项清单、§14.4 一份快速版、§14.5 一份四问），**模板后面必须紧跟一段「为什么每条规则要这么写」的逐条拆解表**——只给模板不给理由，读者改不动它。

**规则二 · 收束章必须显式回指前文，这是它的职责不是偷懒**

第 14 章正文出现了**几十处「见 § X.Y」**，这是刻意的。理由：方法论章的每一条动作背后都有一条前面已讲清的原理，**把原理与动作接上，全篇才是一个整体而不是十四篇独立文章**。

已验证有效的三种回指写法：
1. **建议 + 依据**：「所有编号出处一律当线索去核」→「因为 § 13.1 的幻觉机制」
2. **同一原理换场景**：§10.1 的「约束要写成正面指令」在 §14.2 变成写作场景的改写例子；§11.1 的「自主权等于你能承受的损失」在 §14.3 变成代码 L4 级判据
3. **在末节列一张「本章建议 → 依赖哪一章」的对照表**（§14.5 最后那张表），并借它说明「为什么这一章放在最后」

**规则三 · 只用前面章节已核实过的事实，需要新证据时改写成经验性表述**

方法论章几乎必然想引用「某研究表明」来增强说服力，**这是最容易造假的地方**。第 14 章的做法：
- 凡引具体研究、数据、判例，**一律取自第 8/10/11/12/13 章已逐条核实的内容并注明「见 § X.Y」**（本章因此复用了：arXiv 2509.04664 的评分制度分析、公平性不可能定理 arXiv 1609.05807 + Chouldechova、pass^k 的 0.95^8=0.66、Thomson Reuters v. Ross 的上诉中状态、Bartz v. Anthropic 的和解结案、Gender Shades 的 `up to`、NIST 那处误引纠正、Anthropic Computer Use 的注入原文、GPT-3 论文 10~100 vs Anthropic 3~5、Lost in the Middle 的常见误读、GSM8K 71.8 vs 58.8）
- 需要外部证据但没有的，**改成经验性表述**：「实践中常见的情况是……」「本节属经验性观察而非研究结论」
- **学习科学类说法（费曼学习法、主动回忆、间隔重复、流畅性错觉、自动化偏差）一律作为通行方法/通行概念介绍，明确不给论文引用**，正文写「本文按通行方法介绍其做法，不为它们编造论文出处」

**规则四 · 态度章的基调：边界不建立在能力上，建立在性质上**

第 14 章最重要的一处论证结构：**「不该外包」的理由不能是「它做不好」**——因为能力会变，按那个逻辑它哪天做得比人好边界就消失了。所以四条标准全部**只依赖决定本身的性质**。可复用的讲法：「为什么法官必须是人？不是因为算法查不清法条（机器更快），而是因为判决里包含一样东西——有人为它负责、可被追责、可被上诉、可被要求解释。**这不是知识，是制度位置。能力可以外包，责任不能外包。**」

**规则五 · 一定要写「接管的是那一环，不是整件事」**

方法论章天然容易把读者推向过度反应（「那我不用了」）。**每一条风险提示后面必须紧跟适用范围**。第 14 章的处理：
- §14.3 明写 Vibe Coding 的**五类场景它是最优选择**，并指出真正的问题是「你以为在租三天结果住了二十年」——**不是它不好，是代码性质变了却没补检查**
- §14.4 末尾单开一段「别把批判性变成不用」，并给出四条理由（验证成本低于重做成本 / 不用也有持续代价 / 方法本身低成本 / 验证能力可增长且最保值）
- §14.5 的四问检查表最后一句是**「命中任意一条 → 那一环由人接管，其余环节照常交给它」**，并用招人举例（录用与拒绝必须是人，整理简历/生成问题/列匹配项照常交出去）

**规则六 · 分级永远优于禁令，标准永远优于清单**

已验证的三处分级：§14.3 的 L0~L4 代码分级、§14.4 定性的六类 + 定责的五档、§14.5 的四条标准。**理由要写进正文**：清单会过期（两年后的新场景不在上面），标准能让读者自己判断新情况。可复用的讲法：「交通规则不可能穷举世上每个路口，所以它教的是看标志、看灯、让行原则。」

另一条同源的：**「一套只在需要时才启动的检查，才是你真的会执行的检查」**——§14.4 刻意让第 1 步定性就放行大部分情况，因为要求「时时保持警惕」的方案一定会失败，人的警惕是有限资源。安检类比把这层讲透了（不是对所有人开箱，是按类别执行固定动作；且安检员的职责是按流程执行而非凭感觉看谁像坏人——**因为坏人不会长得像坏人，正如假话不会说得像假话**）。

**规则七 · 篇末节的 pager 指向下一篇入口**

智能篇最后一节 §14.5 的 next 指向 `../network/01-what-is-network.html`，dir 写「下一章 →」，title 写「网络篇 · 第 1 章 · 网络是什么」。**不要写 disabled，也不要回指首页**——线性阅读路径应当跨篇连通。

**规则八 · 方法论章不必也不该追求「新知识密度」**

`sync_spec.py` 的类比种类阈值（≥5 种）在方法论章反而容易低，因为通篇在讲抽象动作。**对策是把每条建议都落到一个生活场景上**（第 14 章用到的：装修/电钻打孔、健身私教、代打与陪练、水电师傅验收、短租房 vs 住二十年、挂号窗口两句话、机场安检分级、手术室签字、导航与副驾驶与自动驾驶、菜市场挑菜、料包与餐厅、干洗店「只洗不熨」、理发店、超市推车拿药）。**一节 7~15 个类比是正常水位**，反而比技术章更多。



### 8. 推送规范

**每次修改后必须**:
```powershell
git add . ; git commit -m '<type>(<scope>): <description>' ; git push
```

**commit 类型**:
- `feat`: 新功能/新章节
- `fix`: bug 修复
- `style`: 样式调整
- `refactor`: 重构

## 内容时效性维护（★ 2026-08-07 建立）

AI 领域产品迭代极快，**写的时候对、半年后就错**。已建立的做法与本次核查结论：

**已验证有效的三条写法**（让内容抗过期）：
1. **凡是会变的数字都带查询日期**——「截至 2026 年 8 月 6 日」。读者能自己判断新鲜度，且我们不必反复改。
2. **取不到权威来源就主动留白**，写明「未取得」。本站在 ChartQA、Llama/Gemini 上下文窗口、embedding 单价、SWE-bench 2026 SOTA 上都这么做了，核查时被确认为「稳健写法」。
3. **举例加「系」字**——写「DeepSeek V3/V4 系」而不是「DeepSeek-V3」，免疫小版本迭代。

**要避免的三种表述**：
1. **「最新」「当前最强」「旗舰」**——这类词几个月就失效。改成带日期的描述或「官方文档首推」。
2. **排名式断言**（如「用户量最大」）——除非有官方数据，否则违反本站自己的核实原则。已把 §2.5 豆包那处改为「中文场景投入大、迭代快」。
3. **标题与内容自相矛盾**——如「规模一览（截至 2026）」下面举的全是 2024 年型号。已改为「举例以公开数字为准，型号会随迭代变化」。

**2026-08-07 核查结论**：全站 85 篇整体时效性良好，§8.1/§8.2/§8.3/§9.5/§11.6 的价格、上下文窗口、MCP 规范全部逐项对得上官方文档，不需返工。已修订 4 处：
- **电学篇 §1.5 急救章：AHA 2020 → 2025 指南**（2025-10-22 发表于 Circulation，官方明确「supersede」2020 版）。★ 这条最要紧，涉及急救准确性。注意按压频率 100~120/分、深度 5~6 cm、比例 30:2 这三个数字未见 2025 版改动，很可能沿用，但**版本标签必须换**。
- §2.5 主流模型表：GPT-4o/o3/o4 → GPT-5.6 Sol/Terra/Luna；Gemini 1.5/2.0 → 3.6/3.5 系（**2.0 官方已标注关停**）；DeepSeek V3/R1 → V4-Flash/V4-Pro（旧 deepseek-chat/reasoner 已于 2026-07-24 弃用）
- §2.5 规模表标题、豆包排名断言
- §7.5 MoE 举例加「系」字

**下次需要复查的时间点**：
- **2026-09-24：Sora API 停止服务**。届时 `09-5-speech-video.html` 里「API 尚在服务」的表述、第 240 行代码块、quiz 选项都要改成「已全面停运」。这是全站唯一有明确到期日的内容。
- 各家模型换代时复查 §2.5、§7.5、§8.1~8.3 的型号与价格。
- **待补核实**：ElevenLabs 定价（$6/月 Starter、$11–22/月 Creator）本次未取得官方页面；`o3 / o4-mini` 主线模型能否直接调用未取得明确结论。

**一个值得写进正文的教学点**：OpenAI 官方文档自身不一致——`developers.openai.com` 首推 GPT-5.6 Sol，而 `platform.openai.com` 仍写「use gpt-5.5, our flagship model」。这与 §11.6 处理 MCP 版本号矛盾（versioning 页写 2025-11-25、latest 解析到 2026-07-28）手法同源，都能教读者「官方文档也会自相矛盾，要并列呈现而非挑一个」。

## 常见 bug 记录

### Bug 1: 悬浮翻页岛不显示
**原因**: 新页面忘了引入 `float-pager.js` 或没有 `.pager` 元素。
**修复**: 检查页面底部是否有 `<script src="../../_shared/js/float-pager.js"></script>` 和 `<div class="pager">`。

### Bug 2: 知识树点击不能展开/收起
**原因**: `expandAndCollapse: true` 导致 ECharts 内部状态和数据不同步。
**修复**: 已改为 `expandAndCollapse: false` + 手动 toggle `collapsed`。

### Bug 3: 展开后 zoom 倒退
**原因**: `setOption` 时 ECharts 用旧数据重绘。
**修复**: 每次 `setOption` 都传入最新 `data`。

### Bug 4: 默认展开不生效
**原因**: `initialTreeDepth` 被数据中的 `collapsed: true` 覆盖。
**修复**: 渲染后强制执行 `expandToDepth(2)`。

### Bug 5: 认知树默认缩放太小
**原因**: SVG 有两层缩放叠加——`viewBox="0 0 worldW worldH"` 让 SVG 自动缩放内容，同时 `scene transform: scale(x)` 又缩放一次，两者叠加导致我的 scale 失效。
**修复**: SVG `viewBox` 改用**画布像素尺寸** (`0 0 clientW clientH`)，让 `scene transform` 完全掌控缩放。同时初始化用 `requestAnimationFrame × 2 + setTimeout 200ms 兜底`，确保 SVG 布局完成。

## 用户特殊要求记录

- **2026-07-26**: 知识树标签不要白色背景方块（黑暗模式友好）
- **2026-07-26**: 悬浮翻页岛要磨砂玻璃效果，不要贴底横条
- **2026-07-26**: 知识树默认展开到二级
- **2026-07-26**: 每个新页面必须有悬浮翻页岛（引入 float-pager.js）
- **2026-07-26**: 知识树圆点要大要明显（symbolSize: 14, borderWidth: 2）
- **2026-07-26**: 点击圆点 = 展开/收起，点击文字 = 跳转篇章（必须区分）
- **2026-07-26**: 圆点颜色区分——有 link 的节点浅绿色，无 link 的叶子节点浅灰色
- **2026-07-26**: 简化交互——点击节点有 link 跳转，无 link 展开/收起（不再区分圆点/文字）
- **2026-07-26**: 圆点必须不透明，不能透过圆点看到线条
- **2026-07-26**: 有子节点的节点，折叠时 label 显示 `[+]`，展开时显示 `[-]`
- **2026-07-26**: 交互方式改为——单击展开/收起，双击跳转（ECharts 无法区分 symbol/label 点击，用单击/双击分离）
- **2026-07-26**: 树结构优化——如果子篇章已合并成一篇完整文章，树上只保留一个链接节点（如 OSI 七层、简史）
- **2026-07-26**: **重大重构**——放弃 ECharts，改用数据驱动的 SVG 自绘认知树，遵循 COGNITION_TREE_FORMAT_SPEC 规范。数据在 `assets/tree-data.js`（node: id/label/desc/time/children/href/color），渲染在 `assets/cognition-tree.js`，样式在 `assets/cognition-tree.css`。三区结构：工具栏 + 树画布 + 详情面板。
- **2026-07-26**: 节点右侧状态圆点——**深绿色**（`#556b3d`）= 可跳转（有 href），**浅绿色**（`#d4e2c8`）= 可展开（无 href 但有子节点），无圆点 = 叶子节点。不再用 `[+]/[-]` 加号按钮。
- **2026-07-26**: 最终方案——只保留**文字前面**的圆点（`.branch-dot`），颜色区分：**深绿**（`#556b3d`）= 可跳转 / **浅绿**（`#d4e2c8`）= 可展开 / **浅灰**（`#d8d4cb`）= 叶子。文字后面的 `.status-dot` 不再显示。
- **2026-07-26**: 认知树默认缩放必须"正好装下"画布——`fitCanvas` 允许放大到 1.4x，留 5% 边距，SVG viewBox 用画布像素尺寸（避免和 scene transform 叠加缩放）
- **2026-08-01**: 新增第三个顶层分支**界面篇**（`#8a5a7a` 紫褐），10 章 GUI/前端/HCI 内容，目录 `chapters/gui/`
- **2026-08-01**: 三个分支的**第三层节点全部补齐**（智能篇 14 章 / 网络篇 10 章 / 界面篇 10 章，共 34 章 · 180+ 节）。未写文章的节点不带 `href`，圆点显示浅灰色；写完后加 `href` 即自动变深绿色。
- **2026-08-01**: 全站统一 **篇 → 章 → 节** 三级命名（见 §0 规范），最小单位是"节"
- **2026-08-01**: 认知树工具栏加**框宽滑块**（120-420px），可自由调节节点框宽度
- **2026-08-01**: **界面篇第 5 章是用户重点学习目标**——从 5 节扩到 **14 节**，覆盖 Python / C++ / C# / Web / Rust 五大技术栈：Tkinter、PySide6/PyQt6、Qt Designer、CustomTkinter、Flet、NiceGUI、Dear PyGui、Streamlit/Gradio、Windows 原生栈、Avalonia/.NET MAUI、Electron、Tauri、打包分发。写这一章时要**多给可运行代码示例**。
- **2026-08-01**: **封面改为简洁印章式**——不要重复堆字。结构：SVG 印章 logo（方章 + 三色水波对应三篇 + 橙点书舟）→ `学海无涯`（只出现一次，字距 0.22em）→ `Xue Hai Wu Ya` 小字 → 三色分隔线 → `智能 · 网络 · 界面`。页脚也精简为`学海无涯 · 2026`。已加同款 SVG favicon（内联 data URI）。

- **2026-08-01**: **要求文章加入互动元素**——讲到特定内容时要提供合适的动手互动，比如讲选颜色就给一个色盘让用户自己选。已建成互动实验室组件库（`_shared/css/interactive.css` + `_shared/js/interactive.js`，18 种组件），并写入 §6.5 规范。**以后每写一节都要考虑配什么互动组件**。
- **2026-08-01**: 互动组件已**覆盖全站三个篇**——组件库从 10 种扩到 18 种（新增智能篇 4 种：tokenizer / temperature / embedding / neuron；网络篇 4 种：subnet / handshake / latency / status-code）。已上线的 53 篇文章全部配好组件，共 67 处。`temperature` / `embedding` / `flexbox` 预留给尚未写的 §8.4 / §9.2 / §3.3。
- **2026-08-01**: **不要频繁触发限流**——批量改文件用脚本一次跑完；验证交互用一次 `browser_evaluate` 批量断言，不要反复抓页面快照。已写入 §7.5。
- **通用**: 用户要求"记住"、"写进规范"、"更新制作规范"时，**必须写入本 skill 文件**，不能只在对话里答应。
- **通用**: 每次改动完成后**主动跑自检 + 提交推送**，不要等用户催。
- **2026-08-01**: **字数标准提高到 3000 字**（原 2500），并**取消所有"建议时间"改为字数**——树数据 `time` 字段更名为 `words`，节点详情面板显示"内容字数"。理由：阅读时间因人而异，字数客观可核对。已批量换算全部 225 个节点。
- **2026-08-01**: 发现**智能篇前 4 章、网络篇全部**字数严重不达标（智能篇 §1.1–§2.4 仅 691–1036 字，网络篇平均 1202 字），需逐节补写至 3000 字以上。
- **2026-08-02**: **已完成 31 篇扩写**（智能篇 CH1/CH2/CH4/CH6 共 19 篇 + 网络篇 CH1/CH2/CH3 共 12 篇），全部从 700–1900 字扩到 5000–9600 字。全站字数从约 8 万涨到 **30 万字**。剩余 11 篇（智能篇 CH5 神经网络 5 篇 + CH7 Transformer 5 篇 + `ai-05-4`）字数在 2508–2740，接近但未达 3000，用户指示**暂缓处理**，留作后续待办。
- **2026-08-02**: ★★ **用户明确两条最高原则**（已写入 §6）：
  1. **完成度第一** —— 先把所有节写完写全，不要因追求单篇完美而拖慢整体。
  2. **字数越多、内容越详细越好，无上限** —— 3000 字只是及格线不是目标。**不要主动压缩篇幅，不要为省 token 写简略**。写作时主动加真实数据、年份人物、论文名、对比表、ASCII 图、代码示例、历史脉络、正反观点、误解澄清。唯一红线是不许灌水。
  3. 因此**禁止**再向用户提"少写点省 token""控制在 3500 字"这类建议。
- **2026-08-02**: ★★★ **用户要求：所有专业名词都要有大白话解释，多用生活中常见环境类比，让不会的人能迅速理解并加深记忆**。已写入 §6.2「大白话与生活类比规范」作为**写作第一准则**，含五条硬规则：术语首次出现当场翻译、每个核心概念配生活场景类比（附 13 个可复用类比对照表）、抽象数字换算成可感知的量、缩写给全称+中文、每篇必有 `callout.analogy` 譬喻框，以及写完每个 h3 的自查三问。**读者假设是"聪明但零基础"——任何需要预备知识才能读懂的句子都不合格。**
- **2026-08-02**: **全站 57 篇已撰写文章全部完成大白话化改造**，`sync_spec.py` 报告"已达标 57 / 待改造 0"。全站字数 53 万。
- **2026-08-07**: **修复知识树的移动端交互 + 全站补暗色模式**。这两件事暴露了几条以后必须守住的规则。

  ### 一、交互功能必须同时绑鼠标与触摸事件
  - **问题**：`assets/cognition-tree.js` 的拖拽只绑了 `mousedown`/`mousemove`/`mouseup`，缩放只绑了 `wheel`。**这四个事件在触摸屏上根本不触发**，所以手机上知识树完全不能拖也不能缩放。
  - **只改 JS 不够，CSS 必须配套**：`.tree-canvas` 要设 `touch-action: none`。不设的话，即便加了触摸事件，浏览器也会把单指拖动当成滚页面、双指捏合当成缩放整页，**手势会被系统抢走**。
  - 已实现：`touchstart` / `touchmove` / `touchend` / `touchcancel`，支持**单指拖动、双指捏合缩放、双击缩放**。`zoom()` 已改为支持传入焦点坐标（`zoom(delta, cx, cy)`），让缩放围绕手指中心而非画布原点。
  - **双指缩放要相对起始状态整体计算**（记下 `pinch.d` 与 `pinch.s`，用 `pinch.s * (nd / pinch.d)` 算目标值），**不要逐帧累乘**，否则会累积误差漂移。
  - **手指按在节点上时要跳过平移逻辑**（`if (inNode(e.target)) { touchDrag = null; return; }`），否则会吞掉节点自己的 `click`，导致点节点没反应。
  - **SVG 元素的 `closest()` 在个别旧版移动浏览器上不可靠**，已改为手写 `inNode()` 沿 `parentNode` 向上遍历并用 `/\bnode\b/` 匹配 `class` 属性。注意 SVG 元素要用 `getAttribute('class')` 而不是 `classList`。
  - **移动端触摸目标不小于 40px**。原工具栏按钮 `padding: 5px 10px` + 11px 字体只有约 22px 高，手机上很难点准，已提到 `min-height: 40px`。
  - 加 `@media (hover: none)` 关掉移动端的 hover 粘滞态。

  ### 二、站点原本没有暗色模式，这是知识树文字看不见的真正原因
  - **根因链条要记住**：`theme.css` 只有一套浅色变量 → 用户看到的「黑色模式」来自**浏览器/系统的强制反转** → **强制反转能改 CSS 背景，但改不了 SVG 里硬编码的 `fill` 值** → 知识树节点文字写死 `#2b2a26`（深墨）与 `#8a8579`（灰），于是深色字压深色底，看不见。
  - **必须加 `<meta name="color-scheme" content="light dark">`**。这是关键一步：声明后浏览器知道站点自己支持深色，就不再强制反转，避免「双重处理」把颜色搞乱。已批量补到全站 98 个 HTML（脚本幂等，插在 `viewport` 之后）。**以后新建页面必须带这一行**。
  - **暗色规则只覆盖颜色变量，绝不动版式与字体**。已在 `theme.css` 的 `:root` 之后加 `@media (prefers-color-scheme: dark)` 覆盖 11 个颜色变量，这样全站页面自动跟随，不用逐页改。
  - 底色用 `#1c1e1b` / `#23261f` / `#282b24` 这类**带暖调的深色而非纯黑**，保留原本的纸感。
  - 额外需要单独处理的四处（纯靠变量覆盖不到）：① `pre`/`code` 底色；② 表格斑马纹（深色下要反向，用 `rgba(255,255,255,.022)`）；③ `mark`/`mark.key` 高亮句（浅色下的淡黄底在深色下太刺眼，改低饱和暖底）；④ `img`/`svg image` 加 `filter: brightness(.92)` 消除发光感。
  - `cognition-tree.css` 需要独立的暗色块，覆盖 `.node-bg` / `.node-title` / `.node-meta` / `.edge` / `.spec-dot` / `.branch-dot` / 详情面板全套。**注意 `.edge` 连线**——浅底上的深色线在深底上同样会消失，很容易漏掉。
  - **状态圆点的白色描边（`stroke: #ffffff`）在深色节点上会很扎眼**，要改成接近底色的深色描边；而「可跳转」的深绿 `#556b3d` 在深底上看不出，要提亮到 `#93b06f` 一类。

  ### 三、★ 一条硬教训：改 CSS 前必须先核实类名和变量是否真实存在
  - 我这次犯过两个「凭空创造」的错误，都在核实环节被抓出来：
    1. 写了 `.node .node-hit { stroke-width: 14px }` 想放大触摸区 —— **`node-hit` 这个类在项目里压根不存在**，节点其实是 `<rect class="node-bg">` 铺满整框、点击区域本来就够大。
    2. 写了 `--ai` / `--network` / `--gui` / `--elec` 四个品牌色变量想在深色下提亮 —— **这四个变量不存在**，四篇的颜色是各页面直接硬编码的（`#4a6d8c` 蓝、`#556b3d` 绿、`#8a5a7a` 紫、`#a0662c` 铜）。
  - **所以定下规则**：写任何 CSS 选择器或变量引用前，先用 grep 确认它在 JS/HTML 里真实存在。具体做法：
    - 类名：搜 JS 里的 `class: 'xxx'` 与 HTML 里的 `class="xxx"`
    - 变量：搜 `--xxx:` 的定义处，不能只看 `var(--xxx)` 的使用处
  - **已建立校验脚本的检查项**（以后改主题必跑）：① CSS 括号配平（去注释后数 `{` `}`）；② 暗色规则引用的每个类名都能在 JS/HTML/浅色规则中找到；③ 暗色块定义的每个变量在浅色 `:root` 里都有对应；④ `color-scheme` 覆盖率 98/98。

  ### 四、验证策略：真机优先于模拟
  - 触摸交互这类改动，**让用户在真机上试比我在无头浏览器里派发合成事件可靠得多**。真机的多点触控事件顺序、浏览器手势拦截行为都难以模拟。
  - 改动是纯增量时（只新增触摸事件、不动原有鼠标逻辑），风险低，可以直接推送让用户验证，但**必须明确告诉用户要留意哪几点**（我这次列了：双指缩放会不会跳、点节点还灵不灵、页面还能不能正常上下滚）。
  - `hostfile_replace_entries: Operation not permitted` 是 SSH 更新 `known_hosts` 的权限警告，**与推送成败无关**，每次都会出现。**不要用 `git push -q`**，那会把 `main -> main` 的成功回显压掉，只剩这行警告，看着像失败。推送后应额外跑 `git ls-remote origin refs/heads/main` 与本地 HEAD 对比确认。

- **2026-08-08**: ★★★ **智能篇第 14 章 · 与 AI 共处已完成 —— 智能篇 14 章至此全部完结**（章总览 + 5 节，约 **5.0 万字**，5 节全部一次通过 `sync_spec.py`）。全站达标 **100 篇**，缓存号升至 `ct-35`。首页智能篇已无「建设中」残留（wip 卡片数 = 0）。**本章确立了 §7.7「方法论章与收束章的写法规范」共八条，以后凡写方法论/态度/收束类章节一律照此执行。**
  - **本章是全站第一个「非技术章」**：不讲原理，只回答「那我到底该怎么用」。写作难点因此完全不同于前 13 章——**不在「怎么不说错」，在「怎么不说空话」**。方法论章的失败方式非常固定：**通篇正确但一句都用不上**。
  - ★**反空话的可操作判据**：写完一条建议就问「这句话的下一句能不能是『具体怎么做』」，答不出来就删。落地办法是每节都给 `<pre><code>` 提示词模板，**且模板后面必须紧跟一张「每条规则堵的是哪个漏洞」的逐条拆解表**——只给模板不给理由，读者改不动它。本章共给出：§14.1 五个模板（费曼式追问基础版+进阶版 / 出题 / 反例 / 对立视角 / 断点检测）、§14.2 四个（第一稿三步法 / 十条挑刺清单 / 事实清单提取 / 难写的邮件+汇报重排）、§14.3 九项代码审查、§14.4 三步验证快速版、§14.5 决定前四问。
  - ★**收束章的核心职责是显式回指前文**。本章正文出现几十处「见 § X.Y」，这是刻意的：**方法论章不发明新知识，它把前面散落的原理接成一套能用的动作**。三种有效写法：① 建议+依据（「出处一律当线索去核」→「因为 §13.1 的幻觉机制」）；② 同一原理换场景（§10.1 的正面指令 → §14.2 的写作改写；§11.1 的「自主权=你能承受的损失」→ §14.3 的代码 L4 判据）；③ **末节列一张「本章建议 → 依赖哪一章」对照表**（§14.5 那张），并借它说明「为什么这一章放在最后」。
  - ★**§14.5 是全篇价值落点，最重要的一处论证结构是：不可外包的理由不能是「它做不好」**——因为能力会变，按那个逻辑它哪天做得比人好边界就消失了。所以四条标准（**责任归属 / 不可逆后果 / 价值判断 / 他人利益**）全部只依赖决定本身的性质。**可复用的讲法**：「为什么法官必须是人？不是因为算法查不清法条（机器更快），而是因为判决里包含一样东西——有人为它负责、可被追责、可被上诉、可被要求解释。**这不是知识，是制度位置。能力可以外包，责任不能外包。**」另一个抓手极实用：**如果你对被影响的那个人只能解释成「AI 建议这样」，这个决定就不该以这种方式做出**——这句解释既不能被反驳也不能被复核（与 §13.2 的「不可解释的依据会让偏见不可发现」同源）。
  - ★**「接管的是那一环，不是整件事」必须写出来**，否则方法论章会把读者推向「那我不用了」。§14.5 四问检查表最后一句就是这条，并用招人举例：命中标准一与四，但整理简历/生成问题/列匹配项照常交出去，**只有「录用与拒绝」这一下必须是人**。§14.4 也为此单开一段「别把批判性变成不用」（四条理由：验证成本低于重做成本 / 不用也有持续代价 / 方法本身低成本 / 验证能力可增长且最保值）。
  - ★**分级优于禁令，标准优于清单**（理由要写进正文：清单会过期，标准能让读者自己判断新情况）。三处已验证的分级：§14.3 代码 **L0~L4**（L4 的判据与前四级不同——**前四级关心「代码好不好」，L4 关心「错了能不能撤回」**）、§14.4 定性六类 + 定责五档、§14.5 四条标准。配套一条：**「一套只在需要时才启动的检查，才是你真的会执行的检查」**——§14.4 刻意让第 1 步定性就放行大部分情况，因为要求「时时保持警惕」的方案必然失败。**安检类比把这层讲透了**：不是对所有人开箱，而是按类别执行固定动作；且安检员的职责是按流程执行而非凭感觉看谁像坏人——**因为坏人不会长得像坏人，正如假话不会说得像假话**。
  - ★**§14.1 的反直觉核心**：「让 AI 把知识讲得特别清楚，可能是对学习最没用的一种用法」——因为**它的解释能力是它最强的地方，也是学习场景里最大的陷阱**（流畅性错觉：材料越好读越容易高估掌握程度）。全节最简洁的操作准则：**你先输出、它后校正 = 学习；它先输出、你后阅读 = 消费。**同样是「总结」和「答题」，谁先动手决定性质。
  - ★**§14.3 的诚实处理**：Vibe Coding **五类场景确实是最优选择**（一次性脚本 / 陌生语言起步 / 探索验证 / 重复改造 / 查用法，共同特征是生命周期短、后果可控、无人维护）。真正的问题写成了一个类比：**「你以为在租三天，结果住了二十年」**——不是它不好，是代码性质变了却没补检查。核心结论：**最危险的不是跑不起来的代码，是「跑得通、看着对、但你不理解」的代码**；九类问题（异常/边界/并发/资源/密钥/SQL/依赖漏洞/权限/语义）**全部在开发环境里沉默**。最后那个 CSV 例子的教训一句话：**六处问题里没有一条是「代码写错了」，全部是「代码正确地实现了一个没被说清的需求」。**
  - ★**§14.4 的机制地基**：语气自信与内容正确**在机制上毫无关系**（§8.2 自回归 + §13.1 的评分制度分析）。但要立刻补一句防止走极端：**它十次里对九次——正因如此第十次才有杀伤力**，`相当于偶尔失灵的红绿灯比彻底坏掉的更危险`。另收录**五种最常见错法的共同结构**：全部是「把一个有范围的结论搬出它的范围去用」，而搬运动作是靠**删掉标明边界的词**完成的（在某数据集上 / 地区法院 / 预期 / 最高 / 初步）——`相当于把药盒上的「儿童每次半片」剪掉「儿童」两个字`。**实操建议：看到任何强结论，先找它的限定词在哪；找不到限定词的强结论八成被剪过。**
  - **本章事实使用完全遵守「不引入新未核实事实」**：所有具体研究/数据/判例一律取自前面章节已核实内容并注明 § X.Y（复用了 arXiv 2509.04664、公平性不可能定理 arXiv 1609.05807 + Chouldechova、pass^k 的 0.95^8=0.66、Thomson Reuters v. Ross 的上诉中、Bartz v. Anthropic 的和解结案、Gender Shades 的 `up to`、NIST 那处误引纠正、Anthropic Computer Use 注入原文、GPT-3 的 10~100 vs Anthropic 的 3~5、Lost in the Middle 常见误读、GSM8K 71.8 vs 58.8）。**学习科学类说法（费曼学习法、主动回忆、间隔重复、流畅性错觉、自动化偏差）一律作为通行方法介绍，正文明写不为其编造论文出处。**
  - **翻页链已跨篇闭合**：§14.5 的 next 指向 `../network/01-what-is-network.html`（dir「下一章 →」，title「网络篇 · 第 1 章 · 网络是什么」）。**篇末节不要写 disabled，也不要回指首页。**
  - **一处小教训**：写代码模板时在 `<pre><code>` 里用了 `**强调**`，被自检脚本的「疑似 Markdown」检查抓出 5 处。**提示词模板里也不许出现 Markdown 语法**，改用「」书名号式强调。自检脚本应保留这一项检查。

- **2026-08-08**: **智能篇第 13 章 · 局限与风险已完成**（章总览 + 5 节，约 **4.9 万字**，全部一次通过 `sync_spec.py`）。全站达标 **95 篇**，缓存号升至 `ct-34`。**本章确立了 §7.6「法律与安全类内容的写法规范」共十一条，以后凡涉法涉安全内容一律照此执行。**
  - **本章的写作难点不在技术，在「怎么不说错」**。法律与安全这两个领域的共同特征是：**流传最广的说法错得最离谱，而且错法很有规律**——把「上诉中」说成「已判决」、把「和解」说成「败诉」、把「特定形式化假设下的结论」说成「永恒真理」、把「某数据集上的误报率」说成「整体准确率」。
  - ★**两处判例状态是本章最容易出错、也最有价值的澄清**：
    1. **Thomson Reuters v. Ross Intelligence** —— 地区法院（D. Del., No. 1:20-cv-613-SB, Judge Stephanos Bibas）2025-02-11 就 fair use 作出**对原告有利的部分简易判决**（Document 770，认定 `Ross infringed 2,243 headnotes`，且 Ross 的 innocent infringement / copyright misuse / merger / scenes a faire 抗辩全部不成立）；**但该案现在第三巡回上诉法院上诉中（No. 25-2153），2026-06-11 已口头辩论、尚未判决**，因此**不构成有约束力的终局先例**，也**不是对所有 AI 训练行为的一般性宣告**（争的是那 2243 条判例摘要这一具体行为）。
    2. **Bartz v. Anthropic** —— N.D. Cal., No. 3:24-cv-05417-AMO，**和解结案（2026-07-20 terminated），不是判决**。和解的三个后果要写清：法院未就实体争议下判断、**通常不确立先例**、**不构成任何一方的责任承认**。三个索赔期限均已过期（索赔 2026-03-30 / 退出反对 2026-02-09 / 重新纳入 2026-03-09）。
  - ★**§13.1 处理了本章最容易被误传的一条**：arXiv 2401.11817（新加坡国立大学，**arXiv 预印本，未见同行评议**）的「幻觉不可消除」。**五条限定必须一起写**：形式化世界 / 依据计算学习理论 / **从形式世界到现实世界那一步是外推、有争议** / 未排除受限任务上做到零幻觉 / 作者自己在讨论缓解手段的效果。**可复用的讲法**：「不存在能测宇宙中任何温度的温度计」这个结论完全正确，但丝毫不影响腋下体温计在 35–42 ℃ 好用——**「一般意义上不可能」和「在你关心的场景里不可靠」是两件事。**
  - ★**§13.2 的数字全部保留了原文限定词**：Gender Shades（PMLR 81:77-91, 2018，同行评议）的 **34.7% 与 0.8% 原文都是 `up to` / `maximum`，是上限不是均值**；同篇的 **IJB-A 浅肤色占 79.6% / Adience 占 86.2%** 才是解释成因的钥匙。NISTIR 8280（NIST 官方，2019-12，DOI 10.6028/NIST.IR.8280，1827 万图像 / 849 万人 / 99 家开发者 189 个算法）说的是**误报率**差 10 到 100 倍以上，**不是整体错误率**；且**一批中国开发的算法这一效应反转**（这条极少被中文报道引用，却最能说明「问题在数据构成不在人脸本身」）。**NIST 自己纠正过一处误引**：`Those studies did not evaluate face recognition algorithms, yet the results have been widely cited to indict their accuracy.` —— **「性别分类」与「人脸识别」是两个任务，混引即错**。
  - ★**「公平性不可能定理」是本章最硬的一堵墙**：Kleinberg/Mullainathan/Raghavan（arXiv 1609.05807，ITCS 2017，同行评议）与 Chouldechova（*Big Data* 2017, 5:153-163）**各自独立证明**——基础比率不同 + 预测不完美时，统计均等 / 均等化几率 / 预测均等无法同时成立。**这是数学不可能性，不是工程取舍**。所以「让 AI 公平」是个没写完的句子，完整说法必须是「优先哪一种、牺牲哪一种」。**医院分诊台三位家属的类比很好用**（三人要求都正当，但算术上不能同时满足）。另附一条反直觉结论：**想测出偏见就必须保留敏感属性——「不看」不等于公平，只等于测不出来**（代理变量：毕业年份代年龄、地铁站代居住区）。
  - ★**§13.3 最实用的一条是「答案分产品线」**：OpenAI 消费级（ChatGPT Free/Plus/Team，ROW Terms 生效 2026-01-01）输入**可能**用于训练；API 与企业产品（Enterprise Privacy 页更新 2026-01-08）写明 `We do not train our models on your data by default`，API 数据自 2023-03-01 起默认不训练、保留最多 30 天、有 ZDR 选项。Anthropic 商业产品默认不训练（文章 7996868）；消费级也不训练（文章 10023580）**但有两个例外**——主动举报材料、明确 opt-in。**实操口诀：在官方隐私页搜 `train` / `retention` / `opt-in` 三个词，并先确认自己用的是哪条产品线。**
  - ★**§13.4 的核心是一个结构性洞察，不是鉴别技巧**：造假与检测的**成本不对称**（造假者只要在某一个检测器上过关、可无限试错、还能拿公开检测器当陪练；检测方要拦住所有造法含未出现的）。所以重心从「检测假的」转向「给真的盖章」。**验钞机 vs 钞票水印**、**普通信 vs 挂号信**两个类比可复用。**最容易被误用的逻辑要点：两条路线都只能证明「有」不能证明「没有」——「没凭证 ≠ 是假的」，反过来「有凭证 ≠ 内容为真」。**另写了「说谎者的红利」（造假技术的存在给所有想否认的人提供了挡箭牌，损害的是真证据的可信度）。
  - ★**§13.5 唯一一节「厂商官方承认无法根治」**：能这么写是因为拿到了 Anthropic Computer Use 文档原文，且它**在同一份文档里两次**建议 `isolate Claude from sensitive data and actions`。**关键教学点：Prompt 注入与 SQL 注入的根本区别**——SQL 有严格文法，参数化查询能在协议层把指令与数据彻底分开，所以能根治；大模型的输入里指令与资料都是自然语言，**没有任何硬性标记能声明「这段绝对不要当命令看」**。因此防御重心必然落在 **LLM01 × LLM08 是乘法关系**上：注入决定「能不能让它听话」，权限决定「听话后后果多大」。**「万事都听的实习生 + 一串万能钥匙」这个类比把最小权限原则讲透了。**
  - **本章 16 处「未取得」全部在正文明写**：FaceForensics++ 与 DFDC 冠军方案在真实世界数据上的下降幅度（**所以一个百分数都没给**）、SynthID 官方局限性说明、Getty v. Stability AI 美国案 docket、Getty v. Stability AI 英国案状态、美国 TAKE IT DOWN Act 是否生效、OpenAI 明确承认注入无法根治的原文、MCP 官方安全警告原文、**OWASP LLM Top 10 最新版本号**（所列十项基于 v1.0/v1.1）、RAG 消除幻觉的量化声明、厂商公布的模型幻觉率、中文语境的大规模偏见测量报告、AI 生成内容著作权归属的跨辖区结论、拟声所需素材时长等。
  - **两处「同一标准内部版本不齐」值得记住**：C2PA 规范**截至 2026-08-08 为 2.4**（中文资料常见的「最新 2.3」已过期），但其 **`Guidance for AI/ML` 一项仍为 2.3**、`Attestations` 为 1.4——与 §12 章那三处「官方内部不一致」是同类教学点。
  - **一处流程教训**：本次在 `chapters/ai/` 下发现一个早期会话残留的 `13-risks.html`（未被任何页面引用、未进 git、缓存号已是 ct-34），说明**中断的会话可能留下孤儿文件**。**新章开工前应先 `Get-ChildItem chapters/<篇>/1X-*` 看一眼有没有同章号的残留**，避免两份文件并存导致后续接线接错。已删除。

- **2026-08-08**: **智能篇第 12 章 · 生态地图已完成**（章总览 + 5 节，约 5.0 万字，全部一次通过 `sync_spec.py`）。全站达标 90 篇，缓存号升至 `ct-33`。
  - **这一章的核心价值是「怎么查资料」，不是「记住型号」**。所有型号与价格一律带「截至 2026 年 8 月 8 日」，8 处查不到的一律明写「未取得」并列出尝试过的地址。
  - ★**三处「官方内部不一致」已写成教学点**，与 §2.5、§11.6 的同类内容呼应：
    1. **OpenAI 三个官方页面三种口径**——`platform.openai.com` 写「use gpt-5.5, our flagship model」，`developers.openai.com` 同日写「use GPT-5.6 Sol, our flagship model」（句式一字不差、只换主角），而 `openai.com/api/pricing` 当天**完全没列 5.6 系列价格**。处理办法：三处原文并列 + 标出处日期，**不挑一个**。并给出实践取舍：写文章两边都写、写代码以能调通的 ID 为准、做预算以定价页为准。
    2. **TRAE 中英文站不一致**——中文站「Work、Code 与 Design **三种**模式」vs 英文站「two modes—Work and Code」；且**计费体系是两套**：中国站积分制人民币（Lite ¥49/2000 分「Work 专属」、Pro ¥99/4000、Pro+ ¥239/12000、Ultra ¥699/40000，均 31 自然日，Seed 系 2.5 折），国际站 Dollar Usage 制美元（Free/$3/$10/$30/$100，含用量 $5/$20/$90/$400）。**官方未公布积分与美元换算率，绝不可混用或换算。**
    3. **Copilot 列 Opus 4.8 vs Anthropic 官方最高 4.7**——这一处与前两处**类型不同**：前两处是「同一主体内部不一致」（并列原文即可），这一处是「不同主体之间」，要**先分清信源身份**：第三方平台清单权威回答「在我这能用到什么」，厂商文档权威回答「规格与价格」。**拿平台清单当依据说「厂商已发布 4.8」就是编造。**（TRAE 内置清单里 GLM 已到 5.2、Kimi 到 K3，而厂商文档只到 GLM-4.6、K2.5，属同类现象。）
  - ★**§12.4 讲透了「开放权重 ≠ 开源」**，依据 OSI 的 **OSAID v1.0**：四项自由（Use 且 *without having to ask for permission*、Study 且能 *inspect its components*、Modify、Share）+ 三要素（**Data Information + Code（原文是 train **and** run）+ Parameters**）。OSI 原文「'Open Source models' and 'Open Source weights' must include the data information and code used to derive those parameters」。**拿 Llama 4 逐条量了一遍**：7 亿 MAU 门槛直接撞上第一条自由，训练代码与数据信息均未提供 → **属开放权重，不属开源 AI**。同时明确三点限定：不是说 Llama 不好、Meta 官方自称就是 Community License 从未自称 OSI 开源、这是「按 OSAID」的分类而非价值判断。
  - ★**「厂商宣称 vs 第三方评测」的三层修饰词已教给读者**：`preliminary`（NVIDIA Vera Rubin 规格表自带免责「All values are up to and subject to change」）、`expected` + `peak theoretical`（AMD MI400 的所有对比数字）。**转述时删掉限定词就是数字滥用。**
  - **华为昇腾按要求只写一条有据可查的事实**：在 llama.cpp 官方后端列表中作为 `CANN | Ascend NPU` 被支持，另有 GLM-5.2 模型卡提及 vLLM-Ascend/xLLM/SGLang。**四个官方地址全部失败，任何型号与算力数字一律不给**，并把失败地址列进正文供读者复核。
  - **本章 8 处未取得（已全部在正文明写）**：Llama 是否有更新版本、Qwen/GLM/DeepSeek 三家权重许可原文、Hugging Face 平台规模、Mistral AI 整条、TraeCode CLI 官方文档（中英文站七个地址全败，llms.txt 当日无 cli 章节，**自媒体内容一律不采用**）、Midjourney 定价（人机验证页）、Perplexity 定价（五个地址全败）、豆包价格（HTML 未渲染）、Cursor 的 Pro+/Ultra 数字（官方只给倍数）、CUDA 生态量化规模、FLOPS 标准机构定义。
  - **一个可复用的写作动作**：留白时**把尝试过的地址与失败原因列进正文**。这比「未取得」两个字有用得多——读者能接着查下去。

- **2026-08-07**: **智能篇第 10、11 章已完成**（提示词工程 5 节约 4.7 万字 / AI 智能体 Agent 6 节约 6.1 万字）。全站达标 85 篇。
  - **这两章的核心价值在「反常识」**，事实核实挖出多处流传甚广但站不住的说法，已全部写入正文做澄清：
    1. **角色扮演提示（persona）在客观事实题上没有显著收益** —— arXiv 2311.10054（Findings of EMNLP 2024），162 种角色 × 4 个模型家族 × 2410 道事实题，原文「the effect of each persona can be largely random」。但**措辞必须限定**为"事实类任务上平均收益不显著、事前无法预判"，不能写成"角色扮演无用"（它对语气/风格仍有效），且要**并列呈现** Anthropic 官方仍推荐"Give Claude a role"这一对立信源。
    2. **「深呼吸，一步步想」不是人的洞见，是 LLM 自己搜出来的** —— OPRO 论文（arXiv 2309.03409，Google DeepMind，ICLR 2024）。80.2 分**绑定 PaLM 2-L + GSM8K + zero-shot**，同表 gpt-4 搜出的最佳只有 74.5。且论文 §5.2.3 标题即"语义相似的指令可能产生截然不同的准确率"——"Let's think step by step."（71.8）与"Let's work this out in a step by step way…"（58.8）**差 13 个百分点**。
    3. **few-shot 的示例数量有两个不同答案** —— GPT-3 论文（arXiv 2005.14165）原文是 **10 到 100 个**（塞满上下文窗口，2020 年基准做法）；Anthropic 官方 2026 年建议是 **3–5 个**。**混用等于误引**，必须说明两者回答的是不同问题。
    4. **威胁模型 / 给小费基本无效** —— Wharton 团队实证报告（arXiv 2508.00614，含 Ethan Mollick）。"威胁有效"曾被 Google 联合创始人 Sergey Brin 公开背书，实测不支持。**名人背书 ≠ 实证成立**。注意该报告未取得同行评议证据，引用时写"实证报告"不写"论文"。
    5. **礼貌用语的效果结论尚未收敛** —— 三篇研究方向互相冲突（2402.14531 / 2510.04950 / 2512.12812）。**不要写成定论**，可辩护的交集是"当代模型对语气总体稳健"。必须写出方法学局限：小样本容易检测出虚假效应。
    6. **RAG 原论文自称 "a general-purpose fine-tuning recipe"** —— 即 RAG 本身就是一种微调范式，**把它说成"微调的替代品/对立面"是对原论文的误读**。
    7. **ReAct 在 HotpotQA 上并未显著超越 CoT** —— 原文是 "competitive with"，最佳方案是 ReAct+CoT 组合。且 **ReAct 不是刚性三步循环**，决策类任务中 thought 是稀疏出现的；论文的真正抽象是**扩展动作空间 Â = A ∪ L**，thought 是"不改变外部环境、只更新上下文"的特殊动作。
    8. **多 Agent 提升 90.2% 必须带两个限定** —— Anthropic **内部** eval（非公开基准），且伴随 **15× token 成本**，官方自己明确"编码任务不适用"（most coding tasks involve fewer truly parallelizable tasks than research）。不带限定就是数字滥用。
  - ★**MCP 的时效性陷阱（重要教训）**：`Sampling` / `Roots` / `Logging` 三项已在 **2026-07-28 修订中被正式弃用**（SEP-2577），Client 侧现行能力只有 **Elicitation**。若沿用"Resources / Tools / Prompts / Sampling 四大概念"这一广为流传的写法，**已属过时**。另外规范版本号存在**官方文档自身不一致**（versioning 页写 current = 2025-11-25，latest 解析到 2026-07-28），应两者并列而非挑一个。**这类协议类主题必须每次重新核实，不能凭记忆写。**
  - ★**「实验室基准 vs 产品承诺」要分清**：OpenAI Structured Outputs 的 **100%** 是自家 eval 分数，产品层承诺附带"未拒答 + 未被截断"两个前提。写"在官方评测集上达到 100%"准确，写"官方保证 100% 符合 schema"不准确。同理 OPRO 的 8%/50%、Anthropic 的 30% 都是 **"up to"（最多）不是平均**。
  - ★**Lost in the Middle 的常见误读**：它测的是**关键信息的位置**，不是**提示词的长度**，**不直接证明"提示越长效果越差"**。要论证"过长降效"应引 Anthropic 上下文工程文章（注意力预算、n² 对关系、context rot），且官方强调那是**"性能梯度而非断崖"**。
- **2026-08-07**: **并发经验更新（第三次验证）**。本次用「分批 + 每批 2 个」跑了 6 个 agent（2 核实 + 4 写作），全程无 429。**但 2 个仍是安全上限**，且这次遇到新故障模式，已总结为流程：
  - **故障一：agent 回执丢失**（第 9 章遇到）。**处理方式：先查磁盘文件时间戳判断断点**，往往只需补齐尾部而非重写。第 9 章的 `09-3-clip.html` 就是写了 7276 字但缺 quiz/Recap/pager，补完即达标。
  - **故障二：单次 `Write` 内容过大导致连续失败**（报 `missing field 'content'`，第 11 章遇到，连失 4 次）。**处理方式：派任务时就明确要求「先 Write 建骨架（含前 2~3 个 h3），再用多次 SearchReplace 逐步追加，每次 3~4 个 h3」**。这条已验证有效。
  - **降低 429 的三条实操**：① 我在 agent 运行期间**完全静默**，不插自己的工具调用（此前 429 有一部分是等待时自己查文件造成的叠加）；② 让 agent **自查**代替我逐页检查，省掉几十次读取；③ 我的接线/自检/提交**用 `;` 串成单条命令**，一次往返。
  - ★**校验口径要对齐**：我的临时校验脚本门槛比项目自带 `sync_spec.py` 宽（后者大白话按**每千字比例**算，字数越多要求越高）。**必须以 `sync_spec.py` 为准**，否则会出现"我的脚本 ALL PASS 但项目报待改造"。
- **2026-08-06**: **智能篇第 8 章·大语言模型 LLM 已完成**（6 节 + 章总览，约 4.2 万字）。§8.1 Token 与分词 / §8.2 自回归生成 / §8.3 上下文窗口 / §8.4 采样参数 / §8.5 思维链 CoT / §8.6 推理模型。复用了预留组件 `tokenizer`（§8.1）与 `temperature`（§8.4）。全站达标 69 篇。
  - **写作前做了一轮事实核实**，并按可靠度分三档落笔，这个做法对快速演进领域（LLM）非常必要，后续章节沿用：
    1. **可直接引用**：论文的 arXiv 号/会议/摘要内数字（Lost in the Middle、FlashAttention、Top-p 真实标题、CoT 三篇、投机解码）、厂商官方 model card 与定价页数据、o1 公告与 DeepSeek-R1 的 benchmark 表。
    2. **必须标注为推导/经验值**：`1 token ≈ 4 英文字符`（无官方出处）、KV Cache 显存公式（按定义推导，不给具体 GB）、prefill/decode 术语、`temperature=0 不保证可复现`（实践观察）。
    3. **绝对不写**：无权威来源的榜单分数、厂商未确认的窗口数字（Llama 4 Scout 的 10M、Gemini 3.5 Pro 的 2M 均为传闻）、官方文档与第三方榜单矛盾时的断言（如「Claude Opus 5 最强」——官方只列到 4.7）。
  - **踩到的一个知识陷阱**：「一个汉字 = 2 个 token」是 GPT-3/3.5（cl100k 及更早词表）时代的说法，新词表（o200k）对中文优化后通常 <1，DeepSeek 官方给的是 1 个中文字符 ≈ 0.6 token。已在 §8.1 单开一节做误解澄清。
- **2026-08-06**: **新增 Obsidian 库导出器** `tools/export_obsidian.py`。把全站 HTML 转成 Obsidian 双链笔记库（输出到 `d:\TRAE 工作空间\xuehai-wuya-obsidian`），网页版更新后重跑一次即可全量重建。
  - **格式映射**：`p.lead`→`[!abstract]`、`div.scene`→`[!example]`、`callout.analogy`→`[!tip]`、`callout`→`[!warning]`、Recap→`[!summary]`、`quiz`→`[!question]-`（折叠）、`mark.key`→`==高亮==`、`ul.keylist`→「**粗体键** — 值」、表格→Markdown 表格。
  - **自动抽取四类双向链接**：所属章 / 本章小节（章总览页导航卡片）/ 正文引用（正文里的 `§ X.Y`）/ 相邻节（pager）。并生成分篇 MOC 索引 + 总索引 + `.obsidian/graph.json`（按篇预设配色，Ctrl+G 即关系网）。
  - **转换时踩过的坑（改导出器时注意别回退）**：① `div.scene`/`callout` 内有嵌套子 div，非贪婪正则会在第一个 `</div>` 截断，必须做深度配平；② `<ul class="keylist">` 带 class，只匹配裸 `<ul>` 会整块漏掉；③ 代码块里的 `[[1.,0.]]` 会被 Obsidian 当成内链，要用零宽字符转义；④ 嵌套 `<strong>` 导致粗体配对错乱；⑤ 章总览页导航卡片是 `<a class="card">` 而非 div；⑥ 文件名清洗函数必须**幂等**，否则链接名与文件名对不上会产生重复笔记。
- **2026-08-02**: ★ **新增第四个顶层分支「电学篇」**（`elec`，配色 `#a0662c` 铜橙），**11 章 71 节**，目录规划为 `chapters/elec/`。
  - **动机**：用户对生活中的电一无所知、不敢自己修东西，要学的**不是电气工程理论，而是能真正上手的实用知识**（换灯泡、换开关插座、跳闸排查、判断电器故障、用万用表、知道什么必须叫电工）。
  - **章节**：CH1 安全第一课 / CH2 电的最小认知 / CH3 家里的电路地图 / CH4 从换灯泡开始 / CH5 开关与插座 / CH6 跳闸了怎么查 / CH7 电器不工作怎么判断 / CH8 工具箱与万用表 / **CH9 弱电与网络布线（用户特别要求追加）** / CH10 直流的世界 / CH11 电费、省电与请人。
  - **写作要求（与其他篇不同的特殊点）**：
    1. **安全是第一优先**——每个涉及带电操作的节都要有安全提示块；§1.6「红线清单：绝不自己碰」作为全篇锚点被反复引用。
    2. **必须明确"能自己做"和"必须叫电工"的边界**，不能鼓励用户超出能力范围操作。
    3. **强场景化**：每节都对应真实生活场景，避免纯理论。多给操作步骤、判断顺序、选型对照表。
    4. 电学篇的生活类比可大量用**水管/水压/水表**（讲电压电流）、**马路车流**（讲电流）、**保险丝=保险栓**等。
  - **待做的互动组件（5 个已规划）**：`wire-picker` 电线与空开选型器、`breaker-hunt` 跳闸排查模拟器、`socket-wire` 插座接线练习台、`multimeter` 万用表档位模拟器、`power-bill` 功率与电费账本。备选 `shock-scale` 人体电流反应标尺。
  - **品牌视觉已同步**：Logo 水波从三道加到**四道**（蓝/绿/紫/铜），封面渐变条、印章渐变、`.vol-banner.elec` 均已更新。

### 品牌视觉规范

- **Logo 意象**：方形印章（圆角 14）+ 三道水波（上蓝 `#4a6d8c` 智能 / 中绿 `#556b3d` 网络 / 下紫 `#8a5a7a` 界面）+ 顶部橙点 `#a05a2c`（书舟，"学海泛舟"）
- **禁止**：标题重复出现同一组字（如既写 `学 · 海 · 无 · 涯` 又写 `Xue Hai Wu Ya · 学 · 海 · 无 · 涯`）
- **三色分隔线** `.rule-tri`：96×2px，三等分渐变，用于封面标题下方
- 中文标题用字距（`letter-spacing`）营造高级感，必须配同值 `text-indent` 抵消末字右侧空白以保持视觉居中

## 待办事项

- [ ] **界面篇第 5 章 · 桌面 GUI 工具箱（14 节）—— 用户重点学习目标，优先级最高**
- [ ] **电学篇 11 章 71 节全部待撰写**（结构与首页入口已建好，正文未动）
- [ ] 智能篇 CH5（5 节）、CH7（5 节）扩写至 3000 字（当前 2508–2740，用户已指示暂缓）
- [ ] 界面篇 CH03 布局与排版（6 节）、CH04 事件与交互（6 节）
- [ ] 界面篇 CH06-10（Web 前端 / 前端框架 / 移动端 / HCI / AI 时代界面）
- [x] ~~智能篇第 14 章 · 与 AI 共处~~（2026-08-08 已上线，章总览 + 5 节约 5.0 万字）★ **智能篇 14 章全部完结**
- [x] ~~智能篇第 13 章 · 局限与风险~~（2026-08-08 已上线，章总览 + 5 节约 4.9 万字）
- [x] ~~智能篇第 12 章 · 生态地图~~（2026-08-08 已上线，章总览 + 5 节约 5.0 万字）
- [ ] 网络篇第 3 章剩余节: § 3.4 DNS / § 3.5 IP / § 3.6 其他协议
- [ ] 网络篇第 4-10 章（未上线）
- [x] ~~智能篇第 5-7 章~~（2026-07-26 已上线，各拆 5-6 节）
- [x] ~~界面篇分支骨架~~（2026-08-01 已加入认知树 + 主页）
- [x] ~~界面篇第 1-2 章~~（2026-08-01 已上线，共 2 个章总览 + 10 节正文，每节 3000+ 字）
